/* Concurrency & Threading — 5 topics */

IPREP.addTopic({
  id: 'gcd', domain: 'concurrency',
  title: 'GCD Fundamentals: Queues & QoS',
  summary: 'Serial vs concurrent, sync vs async, quality of service, and keeping the main thread free.',
  cards: [
    { d: 'easy', q: 'Serial versus concurrent queue, and sync versus async. Which axis is which?',
      a: "They are **independent** axes and conflating them is the most common GCD confusion.\n- **Serial vs concurrent** is a property of the **queue**: how many of its blocks may run at once. Serial runs one at a time in FIFO order; concurrent may run many.\n- **Sync vs async** is a property of the **call**: whether the calling thread waits. `sync` blocks the caller until the work finishes; `async` returns immediately.\n\nSo `concurrentQueue.sync` is legal and blocks the caller, and `serialQueue.async` is legal and does not." },
    { d: 'easy', q: 'Why must UI work happen on the main queue?',
      a: "UIKit is not thread-safe. Its internal state, the layout pass, the responder chain and the layer tree all assume single-threaded access, and touching them from a background thread produces corruption, undefined layout, and crashes that reproduce one time in fifty.\n\nThe main queue is a serial queue bound to the main thread, which also runs the run loop that drives event delivery and the commit to the render server. `DispatchQueue.main.async` is how you hop back. Xcode's Main Thread Checker will flag violations at runtime." },
    { d: 'medium', q: 'List the QoS classes and what each is for.',
      a: "Highest to lowest:\n- `.userInteractive`: work that must finish this frame. Animation, event handling. Do almost nothing here.\n- `.userInitiated`: the user is waiting and watching a spinner. Results in seconds.\n- `.default`: unspecified; avoid stating it explicitly.\n- `.utility`: progress-bar work, long downloads, imports. Energy-efficient scheduling.\n- `.background`: invisible maintenance. Prefetching, sync, cleanup. The system may defer it substantially, especially in Low Power Mode.\n\nQoS drives CPU priority, I/O priority and timer coalescing, so it is a battery decision as much as a speed one." },
    { d: 'medium', q: 'What is priority inversion and how does GCD handle it?',
      a: "A high-priority task waits on a resource held by a low-priority task, which the scheduler is not running, so the high-priority task is effectively demoted.\n\nGCD mitigates it with **priority donation**: when a high-QoS block is submitted `sync` to a lower-QoS serial queue, the queue is temporarily raised to the caller's QoS so it drains faster. That helps, but it does not eliminate the problem, and it is one reason `sync` from the main queue to a `.background` queue is a bad idea." },
    { d: 'hard', q: 'What exactly deadlocks, and why does `DispatchQueue.main.sync` from the main thread hang?',
      a: "`sync` says: run this block on the target queue and do not return until it finishes. Called from the main thread targeting the main queue, the main thread blocks waiting for the block, but the block can only run on the main thread, which is blocked. Instant deadlock.\n\nThe general rule: **never `sync` onto a queue you might already be on**. The same hangs on any serial queue calling `sync` onto itself, which happens easily when two helper methods both wrap their work in `queue.sync`." },
    { d: 'hard', q: 'How do you make a function safe to call from any queue without deadlocking?',
      a: "Do not test 'am I on this queue' with a thread comparison; use a queue-specific key:\n\n```\nlet key = DispatchSpecificKey<Void>()\nqueue.setSpecific(key: key, value: ())\nfunc safe<T>(_ work: () -> T) -> T {\n    DispatchQueue.getSpecific(key: key) != nil ? work() : queue.sync(execute: work)\n}\n```\n\nBetter still, design so the question never arises: make the function always `async`, or make the class an `actor`. Reentrancy checks are a smell that the ownership model is unclear." },
    { d: 'medium', q: 'What is thread explosion and how do you prevent it?',
      a: "Submitting many blocks to a **concurrent** queue where each blocks on I/O or a lock. GCD sees blocked threads and spins up more to keep the CPU busy, up to a hard limit of 64 per queue. Past that you get memory pressure from thread stacks, context-switch thrashing, and eventually a hang.\n\nPrevent it with `DispatchSemaphore` as a counting limiter, an `OperationQueue` with `maxConcurrentOperationCount`, or `DispatchQueue.concurrentPerform` for CPU-bound work, which uses exactly the right number of threads. Swift concurrency's cooperative pool avoids the problem structurally." },
    { d: 'medium', q: 'What is `DispatchQueue.concurrentPerform` for?',
      a: "Data-parallel CPU work over a fixed iteration count:\n\n```\nDispatchQueue.concurrentPerform(iterations: n) { i in\n    results[i] = expensive(input[i])\n}\n```\n\nIt blocks until all iterations complete and uses a thread count matched to the active core count, so it cannot cause thread explosion. It is the right tool for image processing across tiles or transforming a large array. It is the wrong tool if the body does I/O, because blocked threads waste cores." },
    { d: 'hard', q: 'What is a target queue and why would you set one?',
      a: "Every queue targets another queue, ultimately a global root queue. Setting `queueA.setTarget(queue: queueB)` funnels A's work through B, which means A inherits B's serialisation.\n\nThe practical use is building a **queue hierarchy**: several logical serial queues all targeting one serial queue, so they are mutually exclusive without each holding a thread. It is also how you apply a QoS ceiling to a whole subsystem. Note the target can only be set before the queue is used." },
    { d: 'hard', q: 'What is `DispatchWorkItem` and what does it give you over a plain closure?',
      a: "A reference-typed, cancellable, wait-able unit of work:\n\n```\nlet item = DispatchWorkItem { search(text) }\nqueue.asyncAfter(deadline: .now() + 0.3, execute: item)\n// later\nitem.cancel()\n```\n\nThat is the standard **debounce** for a search field. Note `cancel()` only prevents the item from **starting**; it does not interrupt work already running, so a long body must check `item.isCancelled` itself. You can also attach `notify` handlers and `wait()` on it." },
    { d: 'medium', q: 'What is the difference between the main queue and the main thread?',
      a: "The main **thread** is an OS thread. The main **queue** is a serial dispatch queue whose blocks are executed on that thread. They are not interchangeable: code can be running on the main thread without being on the main queue, for instance inside a `sync` block dispatched from the main thread to another queue that then runs on the main thread's donated context.\n\nPractically this matters for assertions: `Thread.isMainThread` is the reliable UI check, while `dispatchPrecondition(condition: .onQueue(.main))` is the queue-level check." }
  ],
  quiz: [
    { d: 'easy', q: '`DispatchQueue.main.sync { }` called from the main thread:', choices: ['Runs immediately and inline', 'Deadlocks', 'Runs on a background thread', 'Throws an exception'], correct: 1,
      why: 'The main thread waits for a block that can only run on the main thread. Never sync onto a queue you may already be on.' },
    { d: 'medium', q: 'Serial vs concurrent describes the queue; sync vs async describes:', choices: ['The queue priority', 'Whether the calling thread waits', 'The QoS class', 'Whether the block is retained'], correct: 1,
      why: 'They are independent axes. concurrentQueue.sync blocks the caller; serialQueue.async does not.' },
    { d: 'hard', q: '30 network calls submitted async to a concurrent queue cause a hang. The cause is:', choices: ['Too little memory', 'Thread explosion: blocked threads make GCD spawn more up to the 64-thread limit', 'The queue is serial', 'QoS is too low'], correct: 1,
      why: 'Bound concurrency with a semaphore or OperationQueue.maxConcurrentOperationCount, or use URLSession, which already pools connections.' },
    { d: 'medium', q: 'Which QoS should a background sync that the user cannot see use?', choices: ['.userInteractive', '.userInitiated', '.utility', '.background'], correct: 3,
      why: 'It lets the system defer and coalesce the work for battery, especially in Low Power Mode.' },
    { d: 'hard', q: 'DispatchQueue.concurrentPerform is inappropriate when the body:', choices: ['Is CPU bound', 'Performs blocking I/O', 'Writes to distinct array indices', 'Is called with a large iteration count'], correct: 1,
      why: 'It sizes threads to cores. Blocking I/O leaves cores idle with no extra threads to compensate.' },
    { d: 'medium', q: 'DispatchWorkItem.cancel():', choices: ['Interrupts the running block immediately', 'Prevents the item starting, but a running body must check isCancelled itself', 'Throws CancellationError', 'Only works on the main queue'], correct: 1,
      why: 'Cooperative cancellation. The same model applies to Operation and to Swift Task cancellation.' },
    { d: 'hard', q: 'Priority donation in GCD means:', choices: ['Low priority work is deferred forever', 'A serial queue is temporarily raised to the QoS of a higher-priority sync caller', 'Background queues are promoted at launch', 'QoS is ignored for concurrent queues'], correct: 1,
      why: 'It mitigates priority inversion by draining the queue faster so the high-priority waiter unblocks sooner.' }
  ]
});

IPREP.addTopic({
  id: 'threadsafety', domain: 'concurrency',
  title: 'Thread Safety, Races & Deadlocks',
  summary: 'What a data race is, the tools to prevent one, and how to find them.',
  cards: [
    { d: 'easy', q: 'Define a data race precisely.',
      a: "Two or more threads access the same memory location concurrently, at least one access is a **write**, and there is no synchronisation ordering them. The result is undefined behaviour, not merely an unexpected value: the compiler and CPU may reorder, cache or tear the access.\n\nThis is distinct from a **race condition**, which is a logic bug about ordering, such as a check-then-act that another thread invalidates in between. You can have a race condition with no data race." },
    { d: 'medium', q: 'Compare the common synchronisation primitives on iOS.',
      a: "- **Serial DispatchQueue**: the idiomatic choice. Easy to reason about, supports async, no priority inversion pitfalls beyond the usual.\n- **NSLock / os_unfair_lock**: cheapest for very short critical sections. `os_unfair_lock` is not recursive and does not spin, and it must not be copied.\n- **NSRecursiveLock**: same thread may re-acquire. Usually a design smell.\n- **DispatchSemaphore**: counting, so also a concurrency limiter. Easy to misuse as a mutex.\n- **actor**: compiler-enforced isolation, async access, no manual discipline.\n\nAvoid `@synchronized` in new code and never use the deprecated `OSSpinLock`, which has a real priority-inversion bug." },
    { d: 'medium', q: 'Why is `OSSpinLock` deprecated?',
      a: "It spins in a tight loop waiting for the lock. If the lock holder is a **lower-priority** thread that the scheduler has descheduled, the spinning high-priority thread burns its whole quantum without the holder ever getting a chance to run and release. That is unbounded priority inversion and it can hang the app.\n\n`os_unfair_lock` replaced it: it blocks in the kernel and participates in priority donation, so the holder is boosted." },
    { d: 'hard', q: 'Implement a thread-safe property using a reader-writer pattern.',
      a: "```\nprivate let q = DispatchQueue(label: \"cache\", attributes: .concurrent)\nprivate var _items: [String: Data] = [:]\n\nvar items: [String: Data] {\n    q.sync { _items }\n}\nfunc set(_ v: Data, for k: String) {\n    q.async(flags: .barrier) { self._items[k] = v }\n}\n```\n\nConcurrent reads run in parallel; the barrier write waits for all in-flight reads, runs alone, then lets reads resume. Two rules: **every** access must go through the queue, and the barrier must be on a queue you created, since barriers are ignored on the global queues." },
    { d: 'medium', q: 'What is a deadlock and what are the classic conditions?',
      a: "Two or more threads each waiting on a resource the other holds, so none proceeds. Coffman's conditions: mutual exclusion, hold-and-wait, no preemption, and circular wait. Break any one and deadlock is impossible.\n\nThe practical prevention on iOS is **lock ordering**: if every code path acquires locks in the same global order, circular wait cannot form. The other practical rule is never call out to unknown code while holding a lock, because that code may acquire another lock." },
    { d: 'hard', q: 'What is a livelock, and how does it differ from a deadlock?',
      a: "In a livelock, threads are actively running and changing state but making no progress, typically because each keeps yielding to the other. Two threads that each release their lock on detecting contention and immediately retry, in lockstep, is the canonical example.\n\nIt is worse than deadlock in one respect: CPU is burning, so it looks like work is happening and it drains battery. The fix is randomised backoff or a fixed acquisition order." },
    { d: 'medium', q: 'How do you actually find a data race?',
      a: "**Thread Sanitizer**, enabled in the scheme's Diagnostics tab. It instruments memory accesses and reports the two conflicting stacks with the exact addresses. It costs roughly 5-15x runtime and 5-10x memory, so run it in a dedicated CI job or a focused local session, not always.\n\nAlso: the **Main Thread Checker** for UIKit-off-main, `dispatchPrecondition` assertions in code, and Swift 6's compile-time data race checking, which moves the whole class of bug to build time." },
    { d: 'hard', q: 'Why is `lazy var` not thread-safe, and what is?',
      a: "`lazy var` compiles to a check-then-initialise with no synchronisation, so two threads can both see it as uninitialised and both run the initialiser. You get two instances, and worse, a torn write of the storage.\n\nThread-safe alternatives: a `static let`, which Swift guarantees is initialised exactly once via `dispatch_once` semantics; initialising eagerly in `init`; or guarding the lazy access with a lock or queue. `static let` inside a type is the idiomatic thread-safe singleton in Swift, and needs no `dispatch_once` boilerplate." },
    { d: 'medium', q: 'Is `let` on a value type enough for thread safety?',
      a: "For an immutable value type with no reference-type members, yes: concurrent reads of immutable memory are safe by definition. That is the whole appeal of value semantics for concurrency.\n\nIt is **not** enough if the struct contains a class reference, because the referenced object is shared mutable state. And a `let` on a class only makes the reference constant, not the object; every mutable property of that object still needs synchronisation." },
    { d: 'hard', q: 'What is the atomicity trap with `@property (atomic)` in Objective-C?',
      a: "`atomic` guarantees only that an individual get or set is not torn. It says nothing about **compound** operations. `self.count = self.count + 1` is a read, an add and a write, and two threads can interleave and lose an increment despite the property being atomic.\n\nSo `atomic` is not thread safety, it is a very weak per-access guarantee, and it costs a lock on every access. That is why the convention is `nonatomic` everywhere plus real synchronisation at the level of the operation you care about." }
  ],
  quiz: [
    { d: 'medium', q: 'A data race requires:', choices: ['Two threads reading the same memory', 'Concurrent access where at least one is a write, with no synchronisation', 'Two threads on the same queue', 'A lock held too long'], correct: 1,
      why: 'Concurrent reads alone are safe. The write plus missing ordering is what makes the behaviour undefined.' },
    { d: 'hard', q: 'OSSpinLock was deprecated because:', choices: ['It was too slow', 'A spinning high-priority thread can starve a lower-priority lock holder', 'It leaked memory', 'It was not recursive'], correct: 1,
      why: 'Unbounded priority inversion. os_unfair_lock blocks in the kernel and participates in priority donation instead.' },
    { d: 'medium', q: 'In a reader-writer queue, writes use:', choices: ['sync', 'async with .barrier flags on a queue you created', 'sync with .barrier on a global queue', 'A semaphore'], correct: 1,
      why: 'The barrier waits for in-flight reads, runs alone, then resumes concurrency. Barriers are ignored on global queues.' },
    { d: 'medium', q: '`lazy var` accessed from two threads can:', choices: ['Deadlock', 'Initialise twice and tear the storage', 'Throw', 'Return nil'], correct: 1,
      why: 'There is no synchronisation around the check-then-initialise. static let is the guaranteed once-only alternative.' },
    { d: 'hard', q: 'An `atomic` Objective-C property still loses increments because:', choices: ['atomic only applies to objects', 'Read-modify-write is three operations and atomic only protects each individually', 'The compiler ignores atomic', 'ARC reorders the access'], correct: 1,
      why: 'Atomicity per access is not atomicity per operation. You need a lock or a queue around the whole read-modify-write.' },
    { d: 'medium', q: 'The most direct tool for finding a data race is:', choices: ['Instruments Leaks', 'Thread Sanitizer', 'Main Thread Checker', 'Address Sanitizer'], correct: 1,
      why: 'TSan reports both conflicting stacks. Main Thread Checker catches UIKit-off-main, which is a different bug class.' },
    { d: 'hard', q: 'The practical prevention for deadlock in a codebase with several locks is:', choices: ['Use recursive locks everywhere', 'Establish a global lock acquisition order', 'Use spin locks', 'Reduce QoS'], correct: 1,
      why: 'A consistent order breaks the circular-wait condition, which is the one Coffman condition you can actually enforce by convention.' }
  ]
});

IPREP.addTopic({
  id: 'groupsbarriers', domain: 'concurrency',
  title: 'Dispatch Groups, Barriers & Semaphores',
  summary: 'Coordinating multiple pieces of async work without blocking the UI.',
  cards: [
    { d: 'easy', q: 'What is a DispatchGroup for, and how do you use it?',
      a: "Waiting for a set of async tasks to all finish.\n\n```\nlet group = DispatchGroup()\nfor url in urls {\n    group.enter()\n    fetch(url) { _ in group.leave() }\n}\ngroup.notify(queue: .main) { self.render() }\n```\n\nUse `notify` rather than `wait`: `notify` schedules a callback and does not block, while `wait` blocks the calling thread and must never be called on the main queue." },
    { d: 'medium', q: 'What are the failure modes of enter/leave?',
      a: "- **Unbalanced leave**: more leaves than enters crashes immediately with an over-release of the group.\n- **Missing leave**: an early `return` on an error path skips the `leave`, and `notify` never fires, so the spinner spins forever.\n- **Leave called twice** on a completion handler that fires twice.\n\nThe defence is `defer { group.leave() }` immediately after the work begins, so every exit path is covered, and one `enter` per `leave` at the same lexical level." },
    { d: 'medium', q: 'What is a barrier block, and on which queues does it work?',
      a: "`queue.async(flags: .barrier) { }` on a **concurrent** queue waits until every previously submitted block has finished, runs alone with nothing else on that queue, then lets subsequent blocks resume concurrently.\n\nCritically, it only works on a concurrent queue **you created**. Barriers submitted to the global concurrent queues are silently downgraded to a normal block, because you do not own those queues and cannot be allowed to stall the whole system." },
    { d: 'hard', q: 'When is a DispatchSemaphore the right tool, and when is it a mistake?',
      a: "Right: as a **counting limiter**. `DispatchSemaphore(value: 4)` around a loop of downloads caps concurrency at four and prevents thread explosion.\n\nMistake: using it to make an async API synchronous. `semaphore.wait()` on the main thread hangs the UI; on a cooperative-pool thread in Swift concurrency it can deadlock the whole pool, because you have blocked a thread that the runtime assumes is never blocked. If you need to bridge async to sync, restructure with `withCheckedContinuation` and `await` instead." },
    { d: 'hard', q: 'What is the difference between `group.wait()` and `group.notify()`?',
      a: "`wait()` **blocks the current thread** until the group empties, optionally with a timeout returning `.timedOut`. `notify(queue:)` registers a block to run when the group empties and returns immediately.\n\nAlways prefer `notify`. `wait` on the main thread freezes the UI and will be killed by the watchdog; `wait` on a cooperative thread can starve the Swift concurrency pool. `wait` is defensible only in a command-line tool or a test." },
    { d: 'medium', q: 'How do you run two independent requests in parallel and combine the results?',
      a: "With GCD, a group with two `enter`/`leave` pairs writing into two optionals, then `notify` combining them. With Swift concurrency it collapses to:\n\n```\nasync let user = fetchUser()\nasync let posts = fetchPosts()\nlet (u, p) = try await (user, posts)\n```\n\n`async let` starts both immediately and suspends only at the `await`. The Swift version also propagates cancellation and errors automatically, which the group version does not." },
    { d: 'hard', q: 'How do you sequence dependent async work without callback pyramids?',
      a: "GCD options: chain in the completion handlers (the pyramid), or use `OperationQueue` with `addDependency`, which expresses the graph declaratively.\n\nSwift concurrency makes it linear:\n\n```\nlet token = try await login()\nlet profile = try await fetchProfile(token)\nlet feed = try await fetchFeed(profile.id)\n```\n\nThe interview point is recognising that dependencies are a **graph** problem: independent work should be parallel (`async let`, or a group), dependent work sequential. Getting that classification right is most of the performance win." },
    { d: 'medium', q: 'What does `asyncAfter` guarantee, and what does it not?',
      a: "It guarantees the block will not run **before** the deadline. It does not guarantee it runs **at** the deadline: the queue may be busy, and the system coalesces timers to save power, with more slack at lower QoS.\n\nSo it is fine for a debounce or a retry backoff, and wrong for anything needing precision, such as audio scheduling or animation timing, where you want `CADisplayLink` or a media-clock-based API." },
    { d: 'hard', q: 'Write a debounce with GCD.',
      a: "```\nprivate var pending: DispatchWorkItem?\nfunc search(_ text: String) {\n    pending?.cancel()\n    let item = DispatchWorkItem { [weak self] in self?.performSearch(text) }\n    pending = item\n    DispatchQueue.main.asyncAfter(deadline: .now() + 0.3, execute: item)\n}\n```\n\nEach keystroke cancels the previous pending item. Throttle is the sibling: instead of cancelling, ignore new calls until a window elapses. Interviewers ask this because it tests `DispatchWorkItem`, cancellation semantics and the difference between debounce and throttle in one question." },
    { d: 'medium', q: 'How do you add a timeout to a group of async work?',
      a: "```\nif group.wait(timeout: .now() + 5) == .timedOut { ... }\n```\n\nbut that blocks. The non-blocking pattern is a `DispatchWorkItem` scheduled with `asyncAfter` that fires the failure path, plus a flag or a cancel so the success path disables it. Both sides must be guarded so exactly one wins.\n\nIn Swift concurrency you race the work against `Task.sleep` inside a `withThrowingTaskGroup` and cancel the loser, which is cleaner because cancellation propagates." }
  ],
  quiz: [
    { d: 'medium', q: 'Barriers submitted to a global concurrent queue:', choices: ['Work normally', 'Are silently downgraded to a regular block', 'Crash', 'Block the entire system'], correct: 1,
      why: 'You do not own the global queues, so you cannot be allowed to stall them. Barriers need a queue you created.' },
    { d: 'medium', q: 'The safest way to guarantee group.leave() on every path is:', choices: ['Call it at the end of the closure', 'Use defer { group.leave() } right after the work starts', 'Call leave twice', 'Use group.wait()'], correct: 1,
      why: 'A missing leave on an error path means notify never fires; defer covers every exit including throws.' },
    { d: 'hard', q: 'Blocking a Swift concurrency cooperative thread with DispatchSemaphore.wait() can:', choices: ['Only slow things down', 'Deadlock the pool, because the runtime assumes those threads never block', 'Raise a compiler error', 'Increase priority'], correct: 1,
      why: 'The cooperative pool is sized to cores and assumes forward progress. Use withCheckedContinuation to bridge instead.' },
    { d: 'easy', q: 'Preferred way to react to a DispatchGroup completing:', choices: ['group.wait() on the main queue', 'group.notify(queue: .main)', 'A polling timer', 'group.wait() with a timeout on the main queue'], correct: 1,
      why: 'notify is non-blocking. wait on the main thread freezes the UI and risks a watchdog kill.' },
    { d: 'medium', q: 'asyncAfter guarantees the block runs:', choices: ['Exactly at the deadline', 'No earlier than the deadline', 'Within 1ms of the deadline', 'On the main thread'], correct: 1,
      why: 'Timer coalescing adds slack, more so at lower QoS. Use CADisplayLink or a media clock when precision matters.' },
    { d: 'hard', q: 'Debounce differs from throttle in that debounce:', choices: ['Runs the first call and ignores the rest in a window', 'Cancels pending work on each new call, running only after quiet', 'Runs every call on a background queue', 'Uses a semaphore'], correct: 1,
      why: 'Debounce waits for silence. Throttle runs at most once per window and drops the rest.' }
  ]
});

IPREP.addTopic({
  id: 'operations', domain: 'concurrency',
  title: 'Operation & OperationQueue',
  summary: 'Object-oriented concurrency: dependencies, cancellation, and when it beats GCD.',
  cards: [
    { d: 'easy', q: 'What does OperationQueue give you that a raw DispatchQueue does not?',
      a: "- **Dependencies**: `b.addDependency(a)` expresses a graph, and the queue resolves the ordering.\n- **Cancellation**: `operation.cancel()` and `queue.cancelAllOperations()`, cooperative via `isCancelled`.\n- **maxConcurrentOperationCount**: a real concurrency cap with no semaphore boilerplate.\n- **Observable state**: `isReady`, `isExecuting`, `isFinished`, KVO-observable, and `queue.operationCount`.\n- **Reusable, testable units**: an `Operation` subclass is an object you can unit test on its own.\n\nIt is built on GCD, so the cost is a thin layer of objects and KVO." },
    { d: 'medium', q: 'What is the difference between a synchronous and an asynchronous Operation?',
      a: "By default, an `Operation` is finished when `main()` returns, which is fine for CPU work. If `main()` kicks off async work such as a network request, the operation reports finished immediately and dependents run too early.\n\nFor async work you override `isAsynchronous` to return true and manage `isExecuting`/`isFinished` yourself, firing the KVO notifications manually with `willChangeValue`/`didChangeValue`. Getting that state machine right is the classic `Operation` interview question." },
    { d: 'hard', q: 'Sketch a correct asynchronous Operation subclass.',
      a: "```\nclass AsyncOperation: Operation {\n    private var _executing = false, _finished = false\n    override var isAsynchronous: Bool { true }\n    override var isExecuting: Bool { _executing }\n    override var isFinished: Bool { _finished }\n\n    override func start() {\n        if isCancelled { finish(); return }\n        willChangeValue(forKey: \"isExecuting\")\n        _executing = true\n        didChangeValue(forKey: \"isExecuting\")\n        main()\n    }\n    func finish() {\n        willChangeValue(forKey: \"isExecuting\"); willChangeValue(forKey: \"isFinished\")\n        _executing = false; _finished = true\n        didChangeValue(forKey: \"isExecuting\"); didChangeValue(forKey: \"isFinished\")\n    }\n}\n```\n\nNote you override `start()`, not just `main()`, and you must not call `super.start()`." },
    { d: 'medium', q: 'How does cancellation work, and what is the responsibility split?',
      a: "`cancel()` sets `isCancelled` to true. That is **all** it does. If the operation has not started, the queue will not start it. If it is already running, nothing stops it: the body must check `isCancelled` at meaningful checkpoints and return early.\n\nSo a long loop should test `isCancelled` each iteration, and an async operation must also cancel its underlying request and then call `finish()`, or the queue will wait forever on an operation that will never complete." },
    { d: 'hard', q: 'What are the failure modes of operation dependencies?',
      a: "- **Cycles**: `a.addDependency(b)` and `b.addDependency(a)` means neither becomes ready and both hang silently, with no diagnostic.\n- **Cross-queue dependencies are legal** and easy to get wrong, since the dependency is on the operation, not the queue.\n- **A cancelled dependency still counts as finished**, so dependents run anyway. If the dependent needs the result, it must check for it rather than assume success.\n- Adding a dependency **after** the operation has started is ignored.\n\nThe third one is the subtle one: cancelling a prerequisite does not cancel the graph below it." },
    { d: 'medium', q: 'How do you pass data between dependent operations?',
      a: "Either give the dependent a reference to the producer and read its result property in `main()`, which is simple but couples them, or use an intermediate 'adapter' operation whose only job is to move the output of one into the input of the next.\n\nEither way the read happens after the producer finished, which the dependency guarantees, so no extra synchronisation is needed for that hand-off. Do not use a shared mutable dictionary; that reintroduces the race the dependency was preventing." },
    { d: 'medium', q: 'When would you pick OperationQueue over Swift concurrency today?',
      a: "- You need to support an OS older than the async/await deployment target, though that is increasingly rare.\n- You have an existing `Operation`-based subsystem and the migration is not worth it.\n- You need KVO-observable progress and queue depth for a UI showing a work queue.\n- You need a **dependency graph** with dynamic edges added at runtime, which task groups express less naturally.\n\nFor new code, structured concurrency is the default: it gives you cancellation propagation, typed errors and no manual state machine." },
    { d: 'hard', q: 'What is `qualityOfService` on an Operation versus on the queue?',
      a: "Both exist. The operation's QoS is a floor, and the queue's applies to operations that do not specify one. GCD may also **promote** an operation's QoS if a higher-priority operation depends on it, so the effective priority is dynamic.\n\n`queuePriority` is a separate axis: it orders **ready** operations within the same queue, whereas QoS affects scheduling against the rest of the system. Candidates routinely conflate them." },
    { d: 'medium', q: 'What does `waitUntilFinished` do and why avoid it?',
      a: "`queue.addOperations(_:waitUntilFinished: true)` blocks the calling thread until they complete. Called on the main thread it freezes the UI, and it undermines the entire point of a queue.\n\nUse a completion barrier instead: `operation.completionBlock`, or a final operation depending on all the others, or `queue.addBarrierBlock` (iOS 13+). Blocking is almost never the right coordination primitive on a client." },
    { d: 'hard', q: 'How would you build an image loader with OperationQueue?',
      a: "One queue with `maxConcurrentOperationCount` tuned to about 4 to 6 for network, plus a decode queue. Each load is an async operation: fetch, then a dependent decode-and-downsample operation, then hand back on the main queue.\n\nKey details to mention: cancel on cell reuse via a per-index-path operation map; check `isCancelled` before the expensive decode; cache the decoded image keyed by URL plus target size; and downsample with `CGImageSourceCreateThumbnailAtIndex` so you never hold a full-resolution bitmap. That combination is what interviewers are listening for." }
  ],
  quiz: [
    { d: 'medium', q: 'An Operation whose main() starts a network request finishes:', choices: ['When the request completes', 'As soon as main() returns, unless you implement isAsynchronous and manage state', 'Never', 'When the queue is cancelled'], correct: 1,
      why: 'The default operation is synchronous. Dependents run too early unless you implement the async state machine.' },
    { d: 'medium', q: 'operation.cancel() on a running operation:', choices: ['Stops it immediately', 'Sets isCancelled; the body must check and return early', 'Throws CancellationError', 'Removes it from the queue and frees it'], correct: 1,
      why: 'Cancellation is cooperative. An async operation must also cancel its request and call finish(), or the queue waits forever.' },
    { d: 'hard', q: 'You cancel operation A. Operation B depends on A. B will:', choices: ['Also be cancelled', 'Run anyway, because a cancelled dependency counts as finished', 'Hang forever', 'Be removed from the queue'], correct: 1,
      why: 'Cancellation does not propagate down the graph. B must check whether A actually produced a result.' },
    { d: 'medium', q: 'queuePriority differs from qualityOfService in that queuePriority:', choices: ['Affects scheduling against the whole system', 'Orders ready operations within the same queue', 'Sets thread count', 'Controls cancellation'], correct: 1,
      why: 'QoS is a system-wide scheduling hint; queuePriority is intra-queue ordering. They are routinely conflated.' },
    { d: 'hard', q: 'Two operations added as dependencies of each other result in:', choices: ['A runtime exception', 'A silent hang, since neither becomes ready', 'One being dropped', 'A compiler error'], correct: 1,
      why: 'There is no cycle detection. Both stay not-ready forever with no diagnostic, which makes it painful to debug.' },
    { d: 'easy', q: 'For a new codebase targeting current iOS, the default concurrency choice is:', choices: ['OperationQueue', 'Structured concurrency with async/await and task groups', 'NSThread', 'Raw pthreads'], correct: 1,
      why: 'It gives cancellation propagation, typed errors and compile-time isolation checking with no manual state machine.' }
  ]
});

IPREP.addTopic({
  id: 'swiftconcurrency', domain: 'concurrency',
  title: 'Swift Concurrency: async/await, Actors & Sendable',
  summary: 'Structured concurrency, actor isolation, the cooperative pool, and migration from GCD.',
  cards: [
    { d: 'easy', q: 'What does `await` actually do?',
      a: "It marks a **suspension point**. The function may suspend there, giving up its thread so the thread can run other work, and resume later, possibly on a **different** thread. It is not a block and it is not a thread hop by itself.\n\nTwo consequences: state can change across an `await`, so re-check assumptions afterwards, and you cannot hold a lock across one. The visible `await` markers are exactly the points where interleaving is possible, which is why they are required syntax." },
    { d: 'medium', q: 'What is structured concurrency and what does it buy you?',
      a: "Child tasks have a lifetime bounded by their parent scope: `async let` and `withTaskGroup` cannot outlive the function that created them, and the compiler enforces it.\n\nThat gives you three things automatically: **cancellation propagates** from parent to children, **errors propagate** out of the scope, and there are no orphaned tasks. `Task { }` is the unstructured escape hatch, and it gets none of that for free, which is why an unstructured task must manage its own cancellation and lifetime." },
    { d: 'medium', q: 'Explain the cooperative thread pool.',
      a: "Swift concurrency runs on a pool sized to the **number of cores**, not the number of tasks. Tasks are expected to suspend rather than block, so a small number of threads can serve many tasks.\n\nThe hard rule that follows: **never block a cooperative thread**. No `semaphore.wait()`, no `DispatchQueue.sync`, no synchronous file or network I/O. Blocking one thread removes a whole core from the pool, and blocking a few can deadlock the app. This is the single most important operational fact about Swift concurrency." },
    { d: 'hard', q: 'What is an actor and what exactly does it guarantee?',
      a: "A reference type whose mutable state is **isolated**: all access goes through the actor's serial executor, so there is never concurrent access to its stored properties. The compiler enforces this by requiring `await` for cross-actor access and rejecting direct reads from outside.\n\nWhat it does **not** guarantee is atomicity across suspension points. An actor method that awaits mid-way can be interleaved with another call, so an invariant that holds before the await may not hold after. That is actor **reentrancy**, and it is the main actor pitfall." },
    { d: 'hard', q: 'Explain actor reentrancy with a concrete bug.',
      a: "```\nactor ImageCache {\n    var cache: [URL: Image] = [:]\n    func image(for url: URL) async -> Image {\n        if let c = cache[url] { return c }\n        let img = await download(url)   // suspension\n        cache[url] = img\n        return img\n    }\n}\n```\n\nTwo callers for the same URL both miss the cache, both suspend at the download, and you download twice. The actor prevented a data race but not duplicated work. The fix is to store the **in-flight Task** in the dictionary before awaiting, so the second caller awaits the same task." },
    { d: 'medium', q: 'What is `@MainActor` and how does it replace `DispatchQueue.main.async`?',
      a: "`@MainActor` is a global actor whose executor is the main queue. Annotating a type, a function or a property guarantees at **compile time** that it is only touched on the main thread, and calls from elsewhere must `await`.\n\nSo instead of remembering to wrap UI updates in `DispatchQueue.main.async` and being caught by the Main Thread Checker at runtime, you mark the view model `@MainActor` and the compiler enforces it. Annotate at the type level for view models and UI code." },
    { d: 'hard', q: 'What is `Sendable` and why does it exist?',
      a: "`Sendable` marks a type as safe to pass across concurrency domains. Value types of `Sendable` members are implicitly `Sendable`; final classes with only immutable `let` properties can be declared `Sendable`; a mutable class cannot be, unless you take responsibility with `@unchecked Sendable` and provide your own synchronisation.\n\nIt is the mechanism that lets the compiler prove there are no data races. In Swift 6 language mode, violations are **errors**, which is what makes compile-time data race safety possible. Under Swift 5 they are warnings you should be fixing now." },
    { d: 'medium', q: 'How do you wrap a callback-based API in async/await?',
      a: "```\nfunc load(_ url: URL) async throws -> Data {\n    try await withCheckedThrowingContinuation { c in\n        legacyLoad(url) { data, error in\n            if let error { c.resume(throwing: error) }\n            else { c.resume(returning: data!) }\n        }\n    }\n}\n```\n\nThe contract is that `resume` must be called **exactly once**. Zero times leaks the task forever; twice traps. Use the `checked` variant in development, which detects both, and consider `withUnsafeContinuation` only after profiling." },
    { d: 'hard', q: 'How does Task cancellation work?',
      a: "It is **cooperative**, like `Operation`. `task.cancel()` sets a flag and propagates to child tasks. Your code must respond: check `Task.isCancelled`, or call `try Task.checkCancellation()` which throws `CancellationError`.\n\nSome APIs cooperate for you: `Task.sleep` throws on cancellation, and `URLSession`'s async methods throw. A tight CPU loop does not, so it must check itself. And note that a cancelled task still runs to completion unless you check, so cancellation without checks is a no-op." },
    { d: 'medium', q: 'When do you use a task group versus `async let`?',
      a: "`async let` when you have a **fixed, small** number of concurrent operations known at compile time, and you want the results as named values.\n\n`withTaskGroup` when the number is **dynamic**, for example one task per element of an array, or when you want to process results as they complete rather than waiting for all. Groups also let you cap concurrency by adding tasks incrementally as earlier ones finish, which is the structured-concurrency answer to thread explosion." },
    { d: 'hard', q: 'How would you migrate a GCD codebase to Swift concurrency incrementally?',
      a: "Bottom-up, boundary first:\n1. Wrap leaf callback APIs with `withCheckedContinuation` so they present an async face.\n2. Mark view models and UI types `@MainActor`, which removes most `DispatchQueue.main.async` calls.\n3. Convert serial-queue-guarded classes into `actor`s, one at a time.\n4. Turn on strict concurrency checking to `minimal`, then `targeted`, then `complete`, fixing `Sendable` warnings at each level.\n5. Replace groups and semaphore-limited loops with task groups.\n\nThe order matters: converting the top first leaves you awaiting into blocking code, which is exactly the cooperative-pool hazard." }
  ],
  quiz: [
    { d: 'medium', q: 'After an `await`, the code resumes:', choices: ['On the same thread, guaranteed', 'Possibly on a different thread, and state may have changed', 'On the main thread', 'On a new thread every time'], correct: 1,
      why: 'That is why you cannot hold a lock across an await and must re-check assumptions afterwards.' },
    { d: 'hard', q: 'Calling DispatchSemaphore.wait() inside an async function is dangerous because:', choices: ['It is slower than await', 'It blocks a cooperative pool thread sized to core count, risking pool deadlock', 'Semaphores are deprecated', 'It cancels the task'], correct: 1,
      why: 'The pool assumes tasks suspend rather than block. Blocking removes a core; blocking several can deadlock the app.' },
    { d: 'hard', q: 'Actor reentrancy means:', choices: ['Two threads can access actor state at once', 'Another call can interleave at a suspension point, so invariants may not hold across an await', 'Actors cannot call themselves', 'Actor methods are always synchronous'], correct: 1,
      why: 'Isolation prevents data races, not logical races. Cache the in-flight Task to deduplicate concurrent work.' },
    { d: 'medium', q: '@MainActor on a view model gives you:', choices: ['Faster UI updates', 'Compile-time guarantee it is only touched on the main thread', 'Automatic retain cycle avoidance', 'Background execution'], correct: 1,
      why: 'It moves the main-thread rule from a runtime checker to the type system.' },
    { d: 'medium', q: 'A continuation must be resumed:', choices: ['At least once', 'At most once', 'Exactly once', 'Never, it resumes itself'], correct: 2,
      why: 'Zero resumes leaks the task forever; two traps. The checked variants detect both during development.' },
    { d: 'hard', q: 'Task.cancel() on a task running a tight CPU loop with no checks:', choices: ['Stops it immediately', 'Has no effect; the loop runs to completion', 'Throws CancellationError at the loop', 'Suspends the task'], correct: 1,
      why: 'Cancellation is cooperative. Check Task.isCancelled or call try Task.checkCancellation() at loop boundaries.' },
    { d: 'medium', q: 'A dynamic number of parallel operations is best expressed with:', choices: ['async let in a loop', 'withTaskGroup', 'DispatchSemaphore', 'Task.detached per item'], correct: 1,
      why: 'async let requires a fixed compile-time set. Groups also let you bound concurrency by adding tasks incrementally.' }
  ]
});
