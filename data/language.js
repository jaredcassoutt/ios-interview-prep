/* Language Fundamentals — 6 topics */

IPREP.addTopic({
  id: 'arc', domain: 'language',
  title: 'ARC, Retain Cycles & weak/unowned',
  summary: 'How Swift and Objective-C manage object lifetime, where cycles come from, and how to break them.',
  cards: [
    { d: 'easy', q: 'What does ARC actually do, and when does it do it?',
      a: "ARC is **compile-time**, not runtime garbage collection. The compiler inserts `retain`, `release` and `autorelease` calls at the points where it can prove ownership changes, using the Objective-C runtime's reference counting.\n- There is no tracing collector and no separate GC thread.\n- Deallocation is **deterministic**: the instant the count hits zero, `deinit` runs.\n- The cost is spread across your code as retain/release traffic, which is why ARC can show up in a Time Profiler trace." },
    { d: 'easy', q: 'What is a retain cycle, and why does ARC not catch it?',
      a: "A retain cycle is two or more objects that hold **strong** references to each other, so neither count ever reaches zero. ARC only counts references, it never walks the object graph looking for unreachable islands, so a cycle simply leaks. Breaking it requires a human decision about which edge is not ownership." },
    { d: 'medium', q: 'Difference between `weak` and `unowned`?',
      a: "- `weak` is **optional** and **zeroing**. When the target deallocates, the reference is set to `nil`. It requires extra bookkeeping via the side table, so it is slightly slower.\n- `unowned` is **non-optional** and **non-zeroing**. Accessing it after the target dies is a crash (trap).\n\nUse `weak` when the other object can legitimately outlive or die first. Use `unowned` only when you can guarantee the target lives at least as long as the referrer, for example a child that can never outlive its parent." },
    { d: 'medium', q: 'How does a zeroing weak reference actually work under the hood?',
      a: "Each object that has ever been weakly referenced gets an entry in a global **side table**, holding the strong count, the weak count and the list of weak reference slots. When the strong count hits zero, the runtime walks that side table entry and nils out every registered slot before deallocating. That extra indirection and locking is why `weak` reads are measurably more expensive than `unowned` or strong reads, and why you should not put a `weak` read in a tight loop." },
    { d: 'medium', q: 'Name the three classic retain cycle shapes in iOS code.',
      a: "1. **Closure captures self** and self holds the closure. A stored completion handler, a Combine sink, a `Timer` block.\n2. **Parent and child both strong.** A view controller holds a view model, the view model holds a back reference to the controller as a strong delegate.\n3. **Delegate declared strong.** Delegates must almost always be `weak var delegate: FooDelegate?`.\n\nA fourth, sneakier one: `NotificationCenter` observers registered with the block API, which retain the block until you remove them." },
    { d: 'medium', q: 'Why must a `weak` delegate property be declared on a class-bound protocol?',
      a: "`weak` only applies to reference types. A bare `protocol FooDelegate` could be adopted by a struct, so the compiler refuses. Declaring `protocol FooDelegate: AnyObject` constrains conformance to classes and lets you write `weak var delegate: FooDelegate?`. It also lets the compiler use a faster class-existential representation." },
    { d: 'hard', q: 'Does `[weak self]` alone always prevent a cycle? When is it not enough?',
      a: "No. `[weak self]` breaks the closure to self edge, but not a cycle that runs through another object. If self holds a `Task` that holds a closure that strongly captures a coordinator that holds self, you still leak. `[weak self]` also does nothing about a closure that is simply never released, for example a repeating `Timer` you never invalidate: the timer is retained by the run loop, so the closure lives forever even though it holds self weakly. Lifetime is a graph question, not a keyword question." },
    { d: 'hard', q: 'When is `[unowned self]` the right call over `[weak self]`?',
      a: "When the closure cannot outlive self and the extra optional unwrap is noise. The canonical safe case is a closure that is executed synchronously and not stored, or one whose only owner is self and is torn down in `deinit`. In practice most teams standardise on `[weak self]` because the failure mode is a silent no-op instead of a crash, and the performance delta almost never matters. Reach for `unowned` when you want a crash to tell you the lifetime assumption broke." },
    { d: 'hard', q: 'What is `autoreleasepool` and when do you still need it in Swift?',
      a: "An autorelease pool holds objects that were returned `+0` (autoreleased) and drains them when the pool exits. UIKit wraps each run loop turn in a pool, so normally you never think about it. You need an explicit `autoreleasepool { }` when a **tight loop creates many temporary Objective-C-backed objects** and you would otherwise accumulate the whole batch before the run loop turns. Classic example: looping over thousands of files building `UIImage` or `NSData`, where peak memory spikes until you wrap the body in a pool." },
    { d: 'hard', q: 'Object is deallocated but you still see the memory held. What are you looking for?',
      a: "Distinguish a **leak** from **abandoned memory**.\n- A leak means no reference remains but the block is unfreed, usually a cycle. Find it with Instruments Leaks or the Xcode Memory Graph Debugger, which draws the cycle.\n- Abandoned memory means something is still legitimately referenced but should not be: an ever-growing cache, an image cache with no eviction, an array of view controllers you never pop, a `NotificationCenter` observer.\n\nThe Memory Graph Debugger plus `malloc` stack logging tells you who holds the reference." },
    { d: 'medium', q: 'What is the ownership rule for `deinit`?',
      a: "`deinit` runs when the strong count reaches zero, before memory is freed, and it runs on whatever thread released the last reference. That is a trap: an object released on a background queue will run its `deinit` there, so any UIKit teardown inside `deinit` is a threading bug. `deinit` cannot be called directly, cannot be `async`, and the superclass `deinit` runs automatically after the subclass one." }
  ],
  quiz: [
    { d: 'easy', q: 'ARC is best described as:', choices: ['A tracing garbage collector that runs periodically', 'Compile-time insertion of retain/release calls', 'A runtime that scans for unreachable objects', 'Manual memory management with helper macros'], correct: 1,
      why: 'The compiler statically inserts the memory management calls. There is no tracing collector, so deallocation is deterministic.' },
    { d: 'medium', q: 'Which declaration is correct for a delegate that must not create a retain cycle?', choices: ['var delegate: FooDelegate?', 'weak var delegate: FooDelegate?  where protocol FooDelegate {}', 'weak var delegate: FooDelegate?  where protocol FooDelegate: AnyObject {}', 'unowned var delegate: FooDelegate!'], correct: 2,
      why: 'weak requires a reference type, so the protocol must be class-bound with AnyObject. Without that constraint the code will not compile.' },
    { d: 'medium', q: 'A repeating Timer created with the block API and `[weak self]` still keeps the view controller alive. Why?', choices: ['weak self does not work inside Timer blocks', 'The run loop retains the timer, and you never invalidated it, so the block and its captures live on', 'Timers always use unowned capture internally', 'The block is copied to the heap, which makes captures strong'], correct: 1,
      why: 'weak self stops the timer from retaining the controller, but the run loop retains the timer forever until you call invalidate(). The controller may deallocate, but the timer keeps firing and the resource leaks.' },
    { d: 'hard', q: 'You want a crash rather than a silent no-op if a lifetime assumption is violated. Which capture do you use?', choices: ['weak', 'unowned', 'strong', 'autoreleasing'], correct: 1,
      why: 'unowned traps on access after deallocation, which surfaces the broken assumption immediately. weak silently becomes nil and the closure body is skipped.' },
    { d: 'hard', q: 'When does an explicit autoreleasepool still earn its keep in Swift?', choices: ['Any time you use closures', 'In a tight loop creating many temporary Objective-C-backed objects', 'When implementing deinit', 'Only in Objective-C code'], correct: 1,
      why: 'The run loop drains the pool once per turn. A long synchronous loop never gives it a chance, so temporaries accumulate and peak memory spikes.' },
    { d: 'medium', q: 'On which thread does deinit run?', choices: ['Always the main thread', 'Always a background thread', 'Whichever thread released the last strong reference', 'The thread the object was created on'], correct: 2,
      why: 'deinit runs synchronously wherever the final release happened. Touching UIKit there is a threading bug.' }
  ]
});

IPREP.addTopic({
  id: 'valueref', domain: 'language',
  title: 'Value vs Reference Types & Copy-on-Write',
  summary: 'Structs vs classes, semantics, performance, and how Swift collections avoid copying.',
  cards: [
    { d: 'easy', q: 'What is the core semantic difference between a struct and a class?',
      a: "A struct has **value semantics**: assignment or passing to a function gives you an independent copy, so nobody else can observe your mutations. A class has **reference semantics**: you copy the pointer, and every holder sees the same instance mutate.\n\nSecondary differences follow from that: classes get identity (`===`), inheritance, `deinit`, and reference counting. Structs get automatic memberwise init and free thread-safety for local copies." },
    { d: 'medium', q: 'Where do struct and class instances actually live?',
      a: "The common answer, stack vs heap, is an oversimplification. A struct is stored **inline in whatever contains it**. A local struct lives on the stack. A struct stored in a class property lives inside that class instance, on the heap. A struct in an array lives in the array's heap buffer.\n\nA class instance is always a heap allocation with a header holding the type and the reference counts. The important consequence is that struct storage costs no separate allocation and no retain/release traffic." },
    { d: 'medium', q: 'Explain copy-on-write and why Swift collections need it.',
      a: "`Array`, `Dictionary`, `Set` and `String` are structs wrapping a heap buffer. Copying the struct copies only the reference, which is cheap. Before any **mutation**, the type checks `isKnownUniquelyReferenced` on the buffer. If more than one owner exists, it deep-copies first, then mutates.\n\nSo you get value semantics with the performance of reference passing, and you only pay for a copy when a shared value is actually written to." },
    { d: 'hard', q: 'How do you implement copy-on-write in your own type?',
      a: "Wrap the heavy payload in a private final class, hold it in a struct, and guard mutation:\n\n```\nfinal class Box { var data: [Int]; init(_ d: [Int]) { data = d } }\nstruct Buffer {\n    private var box: Box\n    mutating func append(_ x: Int) {\n        if !isKnownUniquelyReferenced(&box) { box = Box(box.data) }\n        box.data.append(x)\n    }\n}\n```\n\n`isKnownUniquelyReferenced` requires an `inout` reference to a **class** variable and returns false for Objective-C classes, so the box must be a native Swift final class." },
    { d: 'medium', q: 'What is the classic performance trap when passing large structs around?',
      a: "A large struct is copied by value at every boundary unless the optimiser can prove otherwise. Passing a struct with twenty stored properties through five function calls copies it five times. Fixes, in order of preference: pass it `borrowing`/`inout` where mutation or read-only borrowing is intended, shrink the struct, or wrap the heavy part in a COW box.\n\nIn practice the compiler is good at this for non-escaping calls, so measure before you contort the design." },
    { d: 'medium', q: 'When should you choose a class over a struct?',
      a: "- You need **identity**: two things with the same values are still different objects, like a network session or a view controller.\n- You need **shared mutable state** that many owners must observe.\n- You need inheritance, or you must interoperate with Objective-C or UIKit.\n- You need `deinit` for resource cleanup.\n\nApple's own guidance is struct by default, class when one of the above applies." },
    { d: 'hard', q: 'What are the thread-safety implications of value vs reference types?',
      a: "Value types are not magically thread-safe, but they make safety **achievable**. If each thread has its own copy, there is no shared mutable state and no race. The danger is a value type stored in a shared location: two threads mutating the same `var array` property is a data race, and worse than a torn read, it can corrupt the COW buffer and crash.\n\nReference types share by definition, so every mutable property needs synchronisation: a serial queue, a lock, or an actor." },
    { d: 'medium', q: 'What does `mutating` actually mean on a struct method?',
      a: "It tells the compiler the method may write to `self`, so `self` is passed `inout` rather than as a borrowed constant. Consequences:\n- You cannot call a mutating method on a `let` value.\n- You cannot call one through a protocol existential unless the variable is a `var`.\n- Inside a mutating method you may reassign `self` entirely.\n- Escaping `self` from a mutating method is not allowed, which is why you cannot capture `self` in an escaping closure there." },
    { d: 'hard', q: 'What is the difference between `==` and `===`?',
      a: "`==` is `Equatable` conformance, comparing **values**, and you define what that means. `===` is identity, comparing whether two references point at the same heap allocation, and it only applies to class instances.\n\nAn interview trap: two structs are never identical because they have no identity. And a class can be `Equatable` with `==` returning true for two distinct instances, which is exactly what you want for a model type." },
    { d: 'medium', q: 'Why is `String` a struct, and what does that cost?',
      a: "`String` is a COW struct over a UTF-8 buffer, so it gets value semantics without copying on every pass. The costs are that `String` is not a random-access collection, since a `Character` is an extended grapheme cluster of variable width, so indexing is `O(n)` and you must use `String.Index` rather than `Int`. When you need integer indexing and fixed-width elements, drop to `Array(string.utf8)` or `utf16`." },
    { d: 'hard', q: 'What is a struct that contains a class reference, and why is it dangerous?',
      a: "It is a value type with a reference type inside, so copying the struct copies the pointer, not the object. You get **shallow** value semantics: two copies of the struct look independent but share the inner object, and mutating through one is visible through the other. This is the single most common accidental aliasing bug in otherwise value-oriented code. Either make the inner type a value type, or implement COW deliberately, or document that the type is a reference wrapper." }
  ],
  quiz: [
    { d: 'easy', q: 'Copying a Swift Array of 10,000 elements is:', choices: ['O(n), the buffer is deep-copied immediately', 'O(1) until one of the copies is mutated', 'O(log n)', 'O(1) always, mutation is shared'], correct: 1,
      why: 'Copy-on-write: the copy shares the buffer, and the deep copy happens lazily at the first mutation of a non-uniquely-referenced buffer.' },
    { d: 'medium', q: 'A struct stored as a property of a class instance lives:', choices: ['On the stack', 'On the heap, inline inside the class instance', 'In a separate heap allocation of its own', 'In static storage'], correct: 1,
      why: 'Value types are stored inline in their container. The container here is a heap-allocated class instance, so the struct is on the heap with no separate allocation.' },
    { d: 'hard', q: 'isKnownUniquelyReferenced returns false unexpectedly for your box type. Most likely cause?', choices: ['The box is a struct', 'The box is an Objective-C class or is not final', 'You passed it by value instead of inout', 'The array was empty'], correct: 1,
      why: 'It requires an inout reference to a native Swift class variable and always returns false for Objective-C classes, so the check silently degrades into copying every time.' },
    { d: 'medium', q: 'Two threads append to the same `var items: [Int]` property with no synchronisation. Worst realistic outcome?', choices: ['One append is lost', 'Nothing, arrays are thread-safe', 'Heap corruption or a crash from a torn COW buffer', 'A compiler error'], correct: 2,
      why: 'Concurrent mutation of a COW buffer can corrupt the buffer state itself, not merely lose an element. This is undefined behaviour, not a benign lost update.' },
    { d: 'medium', q: 'Which is NOT a reason to pick a class over a struct?', choices: ['You need reference identity', 'You need inheritance', 'You want cheaper copies of a large payload', 'You need deinit for cleanup'], correct: 2,
      why: 'Cheap copies of a large payload are what copy-on-write gives you inside a struct. Reaching for a class purely for copy cost is usually the wrong trade.' },
    { d: 'hard', q: 'A struct holds a reference to a class. You copy the struct and mutate the inner object through the copy. The original:', choices: ['Is unaffected, value semantics guarantee isolation', 'Sees the change, because only the pointer was copied', 'Triggers a copy-on-write deep copy automatically', 'Fails to compile'], correct: 1,
      why: 'Value semantics are shallow. The struct copy duplicates the reference, so both structs point at one shared object.' }
  ]
});

IPREP.addTopic({
  id: 'protocols', domain: 'language',
  title: 'Protocols, POP & Existentials',
  summary: 'Protocol design, witness tables, `some` vs `any`, and where protocol-oriented programming pays off.',
  cards: [
    { d: 'easy', q: 'What problem does protocol-oriented programming solve that class inheritance does not?',
      a: "Inheritance is single, rigid and vertical: a type gets exactly one superclass and inherits its stored properties whether it wants them or not. Protocols are **horizontal and multiple**: a type can adopt many, they compose, they work on structs and enums, and default implementations in extensions give shared behaviour without shared storage. The practical win is testability, since you can substitute a fake conformance where you could not substitute a superclass." },
    { d: 'medium', q: 'Difference between a protocol requirement and a protocol extension method?',
      a: "A **requirement** declared in the protocol body is dispatched dynamically through the witness table, so the conforming type's override wins. A method that only exists in the **extension** is dispatched statically on the compile-time type, so calling it through the protocol existential runs the extension version even if the concrete type shadows it.\n\nThis is the single most-asked POP gotcha. If you want polymorphism, the method must be a declared requirement." },
    { d: 'medium', q: 'What is a witness table?',
      a: "A protocol witness table is the compiler-generated list of function pointers implementing a protocol's requirements for a given concrete type. It is the protocol analogue of a class vtable. When you call a requirement on an existential, the runtime looks up the implementation in the witness table attached to that value. Static and generic dispatch can often skip this entirely by specialising the call." },
    { d: 'hard', q: 'What is an existential container and why does `any P` cost something?',
      a: "`any P` is boxed in an existential container: three words of inline value buffer plus a pointer to the type metadata plus pointers to the witness tables. If the concrete value fits in three words it is stored inline; otherwise it is **heap-boxed** and the container holds a pointer.\n\nThe costs are the possible allocation, the loss of static type information so calls go through the witness table, and the loss of specialisation and inlining. `some P` has none of these because the concrete type is known at compile time." },
    { d: 'medium', q: '`some P` versus `any P` in one sentence each.',
      a: "- `some P` is an **opaque type**: one specific concrete type, chosen by the implementation, fixed at compile time. The caller does not know which, but the compiler does, so it is as fast as a generic.\n- `any P` is an **existential**: any type conforming to P, possibly different values in the same array, resolved at runtime.\n\nRule of thumb: reach for `some` by default, use `any` only when you genuinely need heterogeneity, such as `[any Drawable]`." },
    { d: 'hard', q: 'Why could you not put a protocol with an associated type in an array before Swift 5.7?',
      a: "A protocol with an `associatedtype` or a `Self` requirement was not a valid type, only a constraint, because the compiler could not give the existential a concrete layout for the associated type. Swift 5.7 introduced constrained existentials, so `any Collection<Int>` is now legal. You still cannot form `any Equatable` usefully, because `==` takes two `Self` values and the compiler cannot prove two boxes hold the same type." },
    { d: 'medium', q: 'How do you give a protocol a default implementation, and what is the risk?',
      a: "Put it in an extension: `extension Greeter { func greet() { print(\"hi\") } }`. The risk is silent behaviour divergence. If `greet()` is not also declared as a requirement, a conforming type that defines its own `greet()` will still get the extension version when called through `any Greeter`. Declare every method you intend to be overridable in the protocol body." },
    { d: 'medium', q: 'How do protocols make code testable in practice?',
      a: "Define the dependency as a narrow protocol, inject it, and substitute a fake in tests:\n\n```\nprotocol HTTPClient { func get(_ url: URL) async throws -> Data }\nfinal class ProfileService {\n    private let client: HTTPClient\n    init(client: HTTPClient) { self.client = client }\n}\n```\n\nKeep the protocol as small as the consumer needs, not as wide as the implementation. A four-method protocol you fully control beats mocking `URLSession`." },
    { d: 'hard', q: 'What is a protocol composition, and when do you use it?',
      a: "`func render(_ x: some Drawable & Identifiable)` requires both conformances without inventing a new protocol. Use it to state exactly the capabilities a function needs at the call site, which keeps protocols small and avoids the fat-interface problem. You can also `typealias Renderable = Drawable & Identifiable` when the combination recurs." },
    { d: 'medium', q: 'What does `@objc` on a protocol change?',
      a: "It makes the protocol visible to the Objective-C runtime, which enables three things Swift protocols cannot do: `optional` requirements, runtime checks with `respondsToSelector:`, and use as a delegate for UIKit classes that call it dynamically. The cost is that the protocol can only be adopted by classes, only use Objective-C-representable types, and dispatch goes through `objc_msgSend` instead of a witness table." },
    { d: 'hard', q: 'Design question: when is POP the wrong tool?',
      a: "When you find yourself writing a protocol with one conformer, a protocol whose only purpose is to enable mocking a type you own, or a hierarchy of protocols with default implementations calling each other. That last one reproduces the fragile base class problem with worse tooling, because you cannot see the override chain. Also avoid `any P` in hot paths where the boxing shows up in a profile. Concrete types and plain functions are still the default." }
  ],
  quiz: [
    { d: 'medium', q: 'A method exists only in a protocol extension, not as a requirement. You call it on `any P` holding a type that redefines it. Which runs?', choices: ['The concrete type version', 'The extension version', 'Whichever is defined last', 'A compile error'], correct: 1,
      why: 'Extension-only methods use static dispatch on the compile-time type, which is the protocol. To get dynamic dispatch, declare the method as a protocol requirement.' },
    { d: 'medium', q: '`some P` as a return type means:', choices: ['Any type conforming to P, varying per call', 'One specific concrete type, hidden from the caller but fixed at compile time', 'A boxed existential', 'A generic parameter chosen by the caller'], correct: 1,
      why: 'Opaque types preserve the concrete type for the compiler while hiding it from the caller, so there is no boxing and full specialisation is possible.' },
    { d: 'hard', q: 'What does an existential container hold?', choices: ['Only a pointer to the object', 'A 3-word inline buffer, type metadata, and witness table pointers', 'A copy of the protocol definition', 'A vtable inherited from NSObject'], correct: 1,
      why: 'Values larger than the inline buffer are heap-boxed, which is the hidden allocation cost of `any P`.' },
    { d: 'medium', q: 'Which capability does @objc add to a Swift protocol?', choices: ['Associated types', 'Optional requirements', 'Value type conformance', 'Generic constraints'], correct: 1,
      why: 'optional requirements come from the Objective-C runtime. The trade is class-only conformance and objc_msgSend dispatch.' },
    { d: 'hard', q: 'Why can you still not usefully form `any Equatable`?', choices: ['Equatable is a class-only protocol', '`==` takes two Self values and the compiler cannot prove two boxes share a type', 'Equatable has an associated type', 'It is allowed since Swift 5.7'], correct: 1,
      why: 'Self requirements in argument position are the remaining restriction. Constrained existentials fixed associated types, not Self-in-parameters.' }
  ]
});

IPREP.addTopic({
  id: 'generics', domain: 'language',
  title: 'Generics & Associated Types',
  summary: 'Type parameters, constraints, specialisation, and how generics differ from existentials at runtime.',
  cards: [
    { d: 'easy', q: 'What do generics buy you over `Any` or a protocol existential?',
      a: "Type relationships the compiler can check. `func first<T>(_ a: [T]) -> T?` guarantees the return type matches the element type. With `[Any]` you would return `Any` and force the caller to downcast, moving a compile-time guarantee to a runtime crash. Generics are also faster, because the compiler can **specialise** the function for each concrete type and inline it." },
    { d: 'medium', q: 'What is generic specialisation?',
      a: "The optimiser generates a dedicated copy of a generic function for each concrete type it is called with, replacing the generic calling convention and witness table lookups with direct calls that can be inlined. Without it, generics run through a slower path passing type metadata and witness tables at runtime.\n\nSpecialisation happens within a module by default. Across module boundaries you need `@inlinable` or whole-module optimisation, which is why a generic in a framework can be slower than the same code in your app target." },
    { d: 'medium', q: 'What is an `associatedtype` and why does it exist?',
      a: "It is a placeholder for a type that the conforming type chooses, letting a protocol describe a family of types rather than one:\n\n```\nprotocol Container {\n    associatedtype Item\n    mutating func append(_ x: Item)\n    var count: Int { get }\n}\n```\n\n`Array<Int>` conforms with `Item == Int`. Generics parameterise a function or type; associated types parameterise a **protocol**." },
    { d: 'hard', q: 'Explain `where` clauses on a generic and on an extension.',
      a: "A `where` clause adds constraints beyond conformance. On a function it restricts callers:\n\n```\nfunc sum<C: Collection>(_ c: C) -> Int where C.Element == Int\n```\n\nOn an extension it adds members that only exist for some instantiations, which is how `Array` gets `sorted()` only when `Element: Comparable`. This is called a **conditional extension** and it is the idiomatic way to layer capability without polluting the base type." },
    { d: 'medium', q: 'What is conditional conformance?',
      a: "`extension Array: Equatable where Element: Equatable` means an array is equatable exactly when its elements are. It lets generic types propagate protocol conformance instead of forcing you to write wrappers. Before Swift 4.1 you could not express this, which is why `[[Int]] == [[Int]]` used to fail to compile." },
    { d: 'hard', q: 'Generic parameter vs `some` parameter, what is the difference?',
      a: "`func f<T: P>(_ x: T)` and `func f(_ x: some P)` are the same thing, the second is sugar for the first. The difference appears only when you need to **name** the type, for example to constrain two parameters to be the same type or to reference `T` in the return type. Use `some` when the type appears once and is not referenced." },
    { d: 'hard', q: 'Why is `[any Shape]` sometimes right and `[some Shape]` never valid?',
      a: "`some Shape` is one fixed concrete type, so an array of them would have to be homogeneous, in which case you would just write `[Circle]`. Heterogeneity is exactly the thing existentials provide, so a mixed collection must be `[any Shape]`. The cost is boxing per element and dynamic dispatch on every call, which is fine for a dozen view models and wrong for a million particles." },
    { d: 'medium', q: 'What is type erasure and when do you write an `AnyX` wrapper?',
      a: "Type erasure hides a generic or associated type behind a concrete box that forwards calls through stored closures. `AnySequence`, `AnyPublisher` and `AnyView` all do this. Before constrained existentials you needed it to store protocol-with-associated-type values in a property or array. Today you often do not, but you still write one when you want to hide implementation types across a module boundary or break a compile-time dependency." },
    { d: 'medium', q: 'How does a generic constraint improve an API you would otherwise write with `Any`?',
      a: "Consider a cache. `func value(for key: Any) -> Any?` forces every caller to cast and cannot enforce that keys are hashable. `final class Cache<Key: Hashable, Value>` moves both concerns to the compiler, gives you autocompletion at the call site, and lets the optimiser specialise the storage. The general lesson: constraints are documentation the compiler enforces." },
    { d: 'hard', q: 'What is the performance model of an unspecialised generic call?',
      a: "The function receives the value indirectly, plus a pointer to the type metadata and one witness table per constraint. Every operation on the value, including copy, destroy and any protocol call, is an indirect call through those tables. No inlining, no register passing for small values. That is typically several times slower than the specialised version, and it is why hot generic code in a framework should be marked `@inlinable`." }
  ],
  quiz: [
    { d: 'medium', q: '`func f(_ x: some P)` is equivalent to:', choices: ['func f(_ x: any P)', 'func f<T: P>(_ x: T)', 'func f(_ x: P.Type)', 'None of these'], correct: 1,
      why: 'some in parameter position is sugar for an unnamed generic parameter. It differs from any P, which boxes the value.' },
    { d: 'medium', q: 'Why can a generic in a framework be slower than the same code in your app?', choices: ['Frameworks disable optimisation', 'Specialisation does not cross module boundaries unless the code is @inlinable or WMO applies', 'Generics are always interpreted', 'The linker strips generics'], correct: 1,
      why: 'Without the body visible across the boundary, the optimiser must emit the unspecialised path with metadata and witness tables passed at runtime.' },
    { d: 'easy', q: 'Which enables `[[Int]] == [[Int]]` to compile?', choices: ['Type erasure', 'Conditional conformance', 'Existential types', 'Associated types'], correct: 1,
      why: 'Array is Equatable only where Element: Equatable, which is a conditional conformance added in Swift 4.1.' },
    { d: 'hard', q: 'You need a heterogeneous collection of shapes. The right type is:', choices: ['[some Shape]', '[any Shape]', '[Shape.Type]', '[AnyObject]'], correct: 1,
      why: 'some Shape is one fixed concrete type, so it cannot be heterogeneous. any Shape boxes each element and dispatches dynamically.' },
    { d: 'hard', q: 'An unspecialised generic call passes which extra arguments at runtime?', choices: ['Nothing extra', 'Type metadata and one witness table per constraint', 'A copy of the protocol definition', 'The caller stack frame'], correct: 1,
      why: 'That indirection is the cost model: no inlining, indirect value access, and a witness table call for every protocol operation.' }
  ]
});

IPREP.addTopic({
  id: 'closures', domain: 'language',
  title: 'Closures, Blocks & Capture Semantics',
  summary: 'Escaping vs non-escaping, capture lists, block storage in Objective-C, and the bugs each causes.',
  cards: [
    { d: 'easy', q: 'What is a closure, precisely?',
      a: "A function plus the environment it captured. The captured variables are stored in a heap-allocated **context** the closure keeps alive, which is why closures can create retain cycles. In Objective-C the same thing is a **block**, and the capture rules differ in one important way: blocks capture by value by default, Swift closures capture by reference." },
    { d: 'medium', q: 'Swift closures capture by reference. What does that actually mean?',
      a: "Capturing a `var` captures the **variable**, not its value at capture time, so later mutations are visible inside the closure and mutations inside are visible outside:\n\n```\nvar n = 0\nlet f = { n += 1 }\nf(); f()\nprint(n)   // 2\n```\n\nTo capture the value at that instant, put it in the capture list: `{ [n] in print(n) }` captures a constant copy. Objective-C blocks are the opposite: they copy by value unless the variable is `__block`." },
    { d: 'medium', q: '`@escaping` versus non-escaping, and why the default matters.',
      a: "A non-escaping closure is guaranteed to finish before the function returns, so it can live on the stack, self can be captured without a cycle risk, and you do not need `self.`. `@escaping` means it may be stored or called later, so it is heap-allocated and captures become ownership.\n\nNon-escaping is the default since Swift 3 specifically because it is cheaper and safer, and it forces you to write `self.` where a cycle is actually possible." },
    { d: 'hard', q: 'Why can a mutating struct method not capture self in an escaping closure?',
      a: "In a mutating method `self` is `inout`, which is a temporary exclusive borrow valid only for the duration of the call. An escaping closure could outlive the call and write to `self` after the borrow ended, which would be a use-after-free of the exclusivity guarantee. The compiler rejects it. The workaround is to capture the specific values you need, or make the type a class." },
    { d: 'medium', q: 'What does a capture list actually do?',
      a: "It evaluates each expression **at closure creation time** and stores the result as a constant in the closure's context. So `[weak self]` stores a weak reference, `[value = self.expensive]` stores a snapshot, and `[x]` stores a copy of `x` that later mutations do not affect. It runs once, when the closure literal is evaluated, not when the closure is called." },
    { d: 'hard', q: 'The `guard let self = self else { return }` dance. What is it doing and when is it wrong?',
      a: "It promotes a weak reference to a strong one for the body's duration, so self cannot deallocate midway and leave you with half-updated state. Since Swift 5.8 you can just write `guard let self else { return }`.\n\nIt is wrong when the closure is long-lived and you actually want it to no-op after teardown, and it is dangerous in a repeating callback because you keep resurrecting a strong reference on every invocation, which can keep a controller alive across a whole scroll session if the callback fires continuously." },
    { d: 'medium', q: 'What is `__block` in Objective-C and what is the Swift equivalent?',
      a: "`__block` marks a local variable as mutable from inside a block and shared with the enclosing scope, moving it from the stack to a heap-allocated box. Swift has no equivalent keyword because Swift closures already capture variables by reference. The Swift analogue of the **opposite** behaviour, capturing by value, is the capture list." },
    { d: 'hard', q: 'Where do Objective-C blocks live, and why does `copy` matter for block properties?',
      a: "A block literal starts on the **stack**. Assigning it to a property or storing it past the current scope requires `Block_copy` to move it to the heap, otherwise you get a dangling pointer once the frame pops. That is why block properties are declared `@property (nonatomic, copy)`. Under ARC the compiler inserts most of these copies automatically, but the `copy` attribute is still the correct and conventional declaration." },
    { d: 'medium', q: 'What is a trailing closure and why does argument order matter?',
      a: "If the last parameter is a closure you can write it outside the parentheses, which is what makes `array.map { }` and `DispatchQueue.main.async { }` read well. Multiple trailing closures were added in Swift 5.3: the first is unlabelled and the rest are labelled. The design lesson is to put the closure last in your own APIs, which is why completion handlers conventionally come last." },
    { d: 'hard', q: 'A completion handler is sometimes called synchronously and sometimes asynchronously. Why is that a bug?',
      a: "It makes reentrancy and locking unpredictable for the caller. If the caller holds a lock or is mid-mutation when it calls your function, a synchronous callback re-enters that state; the same code path with a cache miss goes async and does not. This is the classic 'sometimes-async' hazard: **always** dispatch the callback, or always call it synchronously, and document which. Swift concurrency removes the ambiguity because `await` is always a suspension point." }
  ],
  quiz: [
    { d: 'medium', q: 'In Swift, `var n = 0; let f = { n += 1 }; f(); f(); print(n)` prints:', choices: ['0', '1', '2', 'Compile error'], correct: 2,
      why: 'Swift closures capture the variable itself, not a snapshot, so both calls mutate the same storage.' },
    { d: 'medium', q: 'A non-escaping closure parameter allows you to:', choices: ['Store it in a property for later', 'Omit explicit self and avoid retain-cycle risk', 'Capture self unowned safely', 'Call it from another thread after return'], correct: 1,
      why: 'It is guaranteed to finish before the function returns, so there is no ownership question and no self. requirement.' },
    { d: 'hard', q: 'Why does the compiler reject capturing self in an escaping closure inside a mutating struct method?', choices: ['Structs cannot be captured', 'self is inout, an exclusive borrow that cannot outlive the call', 'Escaping closures require class types', 'It would create a retain cycle'], correct: 1,
      why: 'Escaping past the borrow would let the closure write to self after exclusivity ended, so the compiler forbids it.' },
    { d: 'medium', q: 'Objective-C block properties are declared `copy` because:', choices: ['Blocks are immutable', 'A block literal starts on the stack and must be moved to the heap to outlive its scope', 'copy is faster than strong', 'It prevents retain cycles'], correct: 1,
      why: 'Without the copy the property would point at a stack frame that has already been popped.' },
    { d: 'hard', q: 'A completion handler that is synchronous on cache hit and asynchronous on miss is a bug because:', choices: ['It is slower', 'It makes reentrancy and lock behaviour unpredictable for callers', 'It leaks memory', 'It cannot be tested'], correct: 1,
      why: 'The same call site behaves differently depending on data, so a caller holding a lock can deadlock or re-enter only in one branch.' },
    { d: 'easy', q: '`[weak self]` in a capture list is evaluated:', choices: ['Every time the closure is called', 'Once, when the closure literal is created', 'When self deallocates', 'At compile time'], correct: 1,
      why: 'Capture lists evaluate their expressions once, at closure creation, and store the results in the closure context.' }
  ]
});

IPREP.addTopic({
  id: 'objc', domain: 'language',
  title: 'Objective-C Runtime, Categories & Delegates',
  summary: 'Message dispatch, categories vs extensions, the delegate pattern, and Swift/ObjC interop.',
  cards: [
    { d: 'easy', q: 'What happens when you send a message in Objective-C?',
      a: "`[obj doThing]` compiles to `objc_msgSend(obj, @selector(doThing))`. At runtime the function looks up the selector in the class's method cache, then the method list, then walks the superclass chain. If nothing is found it enters **message forwarding**. Because the lookup is by selector string at runtime, Objective-C can do things Swift cannot: swizzling, `respondsToSelector:`, and adding methods at runtime." },
    { d: 'medium', q: 'What are the three chances to handle an unrecognised selector?',
      a: "1. `+resolveInstanceMethod:` lets you add an implementation dynamically with `class_addMethod`.\n2. `-forwardingTargetForSelector:` lets you return another object to receive the message, which is fast forwarding.\n3. `-forwardInvocation:` with `-methodSignatureForSelector:` boxes the call into an `NSInvocation` you can inspect, modify or forward, which is slow but fully general.\n\nIf all three decline you get `doesNotRecognizeSelector:` and a crash." },
    { d: 'medium', q: 'Category versus Swift extension, what is the real difference?',
      a: "A **category** adds methods to an existing Objective-C class at runtime, including classes you do not own, and the methods are installed into the class's method list when the category loads. It cannot add stored properties, because the instance layout is fixed. Two categories defining the same selector is undefined behaviour: whichever loads last wins, silently.\n\nA **Swift extension** is compile-time. It can add computed properties, initialisers and protocol conformances, and the compiler will reject a duplicate declaration rather than let it race." },
    { d: 'hard', q: 'How do you fake a stored property on a category?',
      a: "Associated objects:\n\n```\nobjc_setAssociatedObject(self, &key, value, OBJC_ASSOCIATION_RETAIN_NONATOMIC);\nobjc_getAssociatedObject(self, &key);\n```\n\nThe runtime keeps a side map from object to key to value, and it is torn down automatically when the object deallocates. It works, but it is slower than a real ivar, invisible to the memory graph tooling, and easy to leak into a cycle if you use `RETAIN` on something that points back." },
    { d: 'medium', q: 'Describe the delegate pattern and its trade-offs versus closures and notifications.',
      a: "A delegate is a one-to-one, named-protocol callback where the delegate object implements methods the source calls. \n- **Delegate**: many related callbacks, clear contract, easy to trace in a debugger. Verbose, and the delegate must be weak.\n- **Closure**: great for a single result. Gets unwieldy past two or three callbacks and invites retain cycles.\n- **Notification**: one-to-many, fully decoupled. Untyped, untraceable, and easy to leak observers.\n\nUIKit uses delegates for `UITableViewDelegate` precisely because there are dozens of related callbacks with a defined lifecycle." },
    { d: 'hard', q: 'What is method swizzling, and what breaks when you do it?',
      a: "Exchanging two method implementations at runtime with `method_exchangeImplementations`, usually to instrument or patch a framework method. Problems: it is global and order-dependent, it breaks if two libraries swizzle the same selector, it must be done exactly once (typically in `+load`, not `+initialize`), and you must call through to the original or you silently drop behaviour. It also confuses crash symbolication. Acceptable for analytics and debugging tools, rarely acceptable in product code." },
    { d: 'medium', q: 'What does `@objc dynamic` do in Swift and what needs it?',
      a: "`@objc` exposes the member to the Objective-C runtime. `dynamic` further forces dispatch **through** `objc_msgSend` rather than a vtable or direct call. You need both for anything that relies on runtime interception: KVO observation of a property, swizzling, and some older framework callbacks. Without `dynamic`, Swift may devirtualise the call and your KVO observer never fires, which is a classic silent bug." },
    { d: 'medium', q: 'What is the difference between `nil`, `Nil`, `NULL` and `NSNull`?',
      a: "- `nil` is a null **object** pointer.\n- `Nil` is a null **class** pointer.\n- `NULL` is a null C pointer.\n- `NSNull` is a real singleton **object** representing null, used because Foundation collections cannot store nil.\n\nThe practical trap: `[dict objectForKey:]` returning `NSNull` is not falsy, so a JSON null becomes an `NSNull` instance that crashes when you message it as a string. In Swift, that surfaces as a decoding failure or an unexpected `NSNull` in a `[String: Any]`." },
    { d: 'medium', q: 'What is unusual about sending a message to `nil` in Objective-C?',
      a: "It is legal and returns zero. `[nil anything]` does nothing and yields 0, nil or a zeroed struct depending on the return type. This is why Objective-C code has fewer nil checks than you would expect, and it is also why bugs hide: a nil receiver silently does nothing instead of crashing. Swift's optionals exist largely to make this explicit, which is why `foo?.bar()` reads as the direct analogue." },
    { d: 'hard', q: 'How does Swift bridge to Objective-C for a class you write?',
      a: "Subclassing `NSObject` or marking members `@objc` generates Objective-C entry points. The generated header (`YourModule-Swift.h`) exposes them. Constraints: generics, structs, enums with associated values, tuples and protocol existentials with associated types cannot cross the boundary. Swift-only types must be wrapped. Bridging also has a cost: `String` to `NSString` and `Array` to `NSArray` conversions can allocate, so avoid them in a hot loop." }
  ],
  quiz: [
    { d: 'medium', q: 'Two categories on the same class define the same selector. What happens?', choices: ['Compile error', 'Both run in load order', 'Whichever category loads last silently wins', 'The runtime raises an exception'], correct: 2,
      why: 'Category method installation is last-writer-wins at load time, with no diagnostic. This is why category collisions are so hard to debug.' },
    { d: 'medium', q: 'Your KVO observer never fires for a Swift property. Most likely missing:', choices: ['@objc only', '@objc dynamic', 'weak', '@available'], correct: 1,
      why: 'Without dynamic the compiler can dispatch directly and skip the runtime hook KVO relies on, so no change notification is emitted.' },
    { d: 'hard', q: 'Which forwarding step is the slowest but most general?', choices: ['+resolveInstanceMethod:', '-forwardingTargetForSelector:', '-forwardInvocation:', 'respondsToSelector:'], correct: 2,
      why: 'forwardInvocation: builds an NSInvocation with boxed arguments, which is expensive but lets you inspect and rewrite the whole call.' },
    { d: 'easy', q: '`[nil doSomething]` in Objective-C:', choices: ['Crashes', 'Returns a zero value and does nothing', 'Throws an exception', 'Is a compile error'], correct: 1,
      why: 'Messaging nil is a no-op returning zero. It hides bugs, which is part of why Swift made nullability explicit.' },
    { d: 'hard', q: 'A category cannot add a stored property because:', choices: ['Categories are compile-time only', 'The instance memory layout is fixed once the class is realised', 'ARC forbids it', 'Properties must be declared in the header'], correct: 1,
      why: 'You cannot grow the ivar layout of an already-realised class, which is why associated objects exist as the side-table workaround.' },
    { d: 'medium', q: 'A JSON null decoded into a `[String: Any]` from JSONSerialization appears as:', choices: ['nil, so the key is absent', 'NSNull, a real object that is not falsy', 'An empty string', 'A decoding exception'], correct: 1,
      why: 'Foundation collections cannot hold nil, so null becomes the NSNull singleton, which crashes if you message it as the expected type.' }
  ]
});
