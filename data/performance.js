/* Performance & Network — 3 topics */

IPREP.addTopic({
  id: 'memoryperf', domain: 'performance',
  title: 'Memory Management & Instruments',
  summary: 'Finding leaks and abandoned memory, image footprint, and the jetsam limits.',
  cards: [
    { d: 'easy', q: 'What is the difference between a leak and abandoned memory?',
      a: "A **leak** is memory with no remaining references that was never freed, almost always a retain cycle. Instruments' Leaks tool finds these because it can see unreachable allocations.\n\n**Abandoned memory** is still referenced, so it is not a leak by definition, but nothing will ever use it again: an unbounded cache, an array of view controllers you never popped, observers never removed. Leaks cannot find it. You find abandoned memory with the Allocations tool and **generation marks**: mark, perform a round trip that should return to the starting state, mark again, and inspect what survived." },
    { d: 'medium', q: 'How do you use the Memory Graph Debugger?',
      a: "Run, reach the suspect state, and click the memory graph button in the debug bar. It snapshots the object graph and shows every instance and who references it. Purple exclamation marks flag detected leaks and cycles.\n\nThe workflow that actually finds bugs: push and pop a screen a few times, snapshot, then filter for your view controller's class name. If more than one instance survives, follow the inbound arrows to whoever is holding it. Enable Malloc Stack Logging in the scheme so each reference shows the stack that created it." },
    { d: 'medium', q: 'How much memory does an image actually use?',
      a: "The decoded bitmap is roughly `width * height * 4` bytes, independent of the file size on disk. A 4000x3000 photo is about **48MB** in memory, even if the JPEG is 2MB.\n\nSo a grid of twenty thumbnails loaded at full resolution is a gigabyte and an immediate jetsam kill. The fix is downsampling at decode time with `CGImageSourceCreateThumbnailAtIndex` and `kCGImageSourceThumbnailMaxPixelSize`, which never materialises the full bitmap. This is the highest-value memory optimisation in most photo-adjacent apps." },
    { d: 'hard', q: 'What is jetsam and what are the practical limits?',
      a: "Jetsam is the memory-pressure killer. iOS has no swap, so when the system needs memory it terminates processes by priority, and a background app goes first. There is no exception and no crash report of the usual kind; you get a `JetsamEvent` log.\n\nLimits vary by device and are not public API, but the practical shape is a few hundred megabytes for a foreground app on older devices, more on recent ones, and much less for an extension. Widgets and share extensions get very tight budgets, often tens of megabytes, which is why an extension that loads a full-size image dies instantly." },
    { d: 'medium', q: 'What is the difference between the Allocations and Leaks instruments?',
      a: "**Allocations** tracks every heap allocation with its stack, and shows persistent versus transient bytes over time. It answers 'what is growing?' and supports generation marks.\n\n**Leaks** periodically scans for allocations with no references and reports them. It answers 'what is definitively unreachable?'\n\nUse Allocations for growth and abandoned memory, Leaks for cycles. In practice a rising Allocations graph with a flat Leaks count is the signature of abandoned memory, and that combination is a great thing to say out loud in an interview." },
    { d: 'hard', q: 'Your app grows by 5MB every time the user opens and closes a screen. Walk through the diagnosis.',
      a: "1. Reproduce deterministically: open and close ten times.\n2. Memory Graph Debugger, filter for the view controller class. More than one live instance means it is retained.\n3. Follow inbound references. Typical culprits: a closure capturing self stored on a long-lived service, a `NotificationCenter` block observer never removed, a strong delegate, a `Timer` never invalidated, or a child coordinator never removed from its parent.\n4. If exactly one instance survives, the growth is elsewhere: use Allocations with generation marks to see which allocations persist across the cycle.\n5. Fix, then re-run the same ten-cycle test and confirm the graph returns to baseline." },
    { d: 'medium', q: 'What is `NSCache` and why prefer it to a dictionary?',
      a: "A mutable key-value cache that is **thread-safe** and **evicts automatically under memory pressure**, with `countLimit` and `totalCostLimit` as soft bounds. It also does not copy its keys, unlike `NSDictionary`.\n\nThe caveats: eviction is opaque and can happen at any time, so it is a cache and never a store of record; the cost you supply is arbitrary and only meaningful if you are consistent; and there is no notification of eviction other than a delegate callback. Use it for decoded images and computed layouts." },
    { d: 'hard', q: 'What is memory footprint versus resident size, and which do you optimise?',
      a: "**Resident size** (RSS) is all physical pages mapped by the process, including shared framework pages and file-backed mappings. **Footprint** is what jetsam actually charges you: dirty, non-purgeable memory attributed to your process.\n\nOptimise footprint. That is why memory-mapped file reads (`Data(contentsOf:options: .mappedIfSafe)`) are cheap: the pages are clean and file-backed, so the kernel can evict and re-read them, and they do not count the same way. Xcode's memory gauge shows footprint." },
    { d: 'medium', q: 'What are the common sources of unbounded memory growth in an iOS app?',
      a: "- An image cache with no limit\n- `NotificationCenter` block observers never removed, each retaining its captures\n- Retained view controllers from coordinator or navigation bugs\n- Accumulating `Timer` or `CADisplayLink` instances\n- An ever-growing array of log or analytics events awaiting flush\n- Core Data contexts never reset, holding every object ever faulted in\n- Autorelease pool not draining in a long synchronous loop\n\nThe Core Data one is worth naming: a long-lived background context accumulates registered objects until you `reset()` or use a fresh context per batch." },
    { d: 'hard', q: 'How do you reduce memory during app launch?',
      a: "- Do not decode images or build view hierarchies for screens the user has not reached.\n- Defer non-critical framework initialisation; each dynamic framework costs load time and dirty pages.\n- Avoid large static data structures constructed at launch; parse lazily.\n- Move work out of `application(_:didFinishLaunchingWithOptions:)` into a post-first-frame phase.\n- Use `Data(contentsOf:options: .mappedIfSafe)` for large resource files so pages stay clean.\n\nMeasure with the App Launch template in Instruments, which shows both time and the allocation timeline in the same trace." }
  ],
  quiz: [
    { d: 'easy', q: 'A 4000x3000 photo decoded into memory costs approximately:', choices: ['2 MB, the JPEG size', '12 MB', '48 MB', '96 MB'], correct: 2,
      why: 'width x height x 4 bytes, independent of the compressed file size. Downsample at decode time to avoid materialising it.' },
    { d: 'medium', q: 'A rising Allocations graph with zero reported leaks indicates:', choices: ['A false positive', 'Abandoned memory: still referenced but never used again', 'A retain cycle', 'GPU memory pressure'], correct: 1,
      why: 'Leaks only finds unreachable allocations. Use Allocations generation marks across a round trip to find what survives.' },
    { d: 'hard', q: 'Jetsam terminates your background app because:', choices: ['It exceeded a CPU limit', 'iOS has no swap, so it kills processes by priority under memory pressure', 'It used too many threads', 'The watchdog timed out'], correct: 1,
      why: 'There is no page-out path, so reclamation means termination. Extensions get far tighter budgets than the main app.' },
    { d: 'medium', q: 'NSCache is preferred over a Dictionary for images because it:', choices: ['Is faster', 'Is thread-safe and evicts automatically under memory pressure', 'Persists to disk', 'Preserves insertion order'], correct: 1,
      why: 'Eviction is opaque, so it is always a cache and never a store of record.' },
    { d: 'hard', q: 'Memory-mapped file reads are cheap in footprint terms because the pages are:', choices: ['Compressed', 'Clean and file-backed, so the kernel can evict and re-read them', 'Stored on the GPU', 'Shared between processes'], correct: 1,
      why: 'Jetsam charges dirty non-purgeable memory. Clean file-backed pages are reclaimable without killing you.' },
    { d: 'medium', q: 'A long-lived Core Data background context grows unboundedly because it:', choices: ['Leaks the store', 'Keeps every faulted-in object registered until reset()', 'Duplicates the persistent store', 'Never saves'], correct: 1,
      why: 'Call reset() between batches, or use a fresh context per unit of work.' }
  ]
});

IPREP.addTopic({
  id: 'renderperf', domain: 'performance',
  title: 'Scroll & Rendering Performance',
  summary: 'The frame budget, hitches, and the concrete causes of jank.',
  cards: [
    { d: 'easy', q: 'What is the frame budget and what does missing it look like?',
      a: "At 60Hz you have **16.7ms** per frame; at 120Hz ProMotion, **8.3ms**. That budget covers your main-thread work, layout, drawing, the commit to the render server, and GPU compositing.\n\nMissing it means the previous frame is shown again, which the user reads as a stutter. Modern terminology calls this a **hitch**, and Apple measures it as hitch time ratio in milliseconds of hitch per second of scrolling. A single 200ms hang is far worse perceptually than twenty 20ms ones." },
    { d: 'medium', q: 'Name the main causes of dropped frames while scrolling.',
      a: "- Synchronous work on the main thread: JSON decoding, disk I/O, image decoding\n- Expensive `layoutSubviews` or heavy Auto Layout on complex cells\n- Custom `draw(_:)` rasterising on the CPU\n- Offscreen rendering from shadows without a `shadowPath`, or masks\n- Blending from non-opaque views stacked deep\n- Allocating views in `cellForRowAt` instead of reusing\n- Blocking on a lock or a semaphore\n\nThe first two account for most real cases." },
    { d: 'medium', q: 'How do you find a scroll hitch with Instruments?',
      a: "Use the **Animation Hitches** template (or the Time Profiler with the main thread filtered). It shows a hitch timeline with the commit phase broken out, so you can tell whether the time went to your main-thread work, the layout and commit, or the render server.\n\nWorkflow: record a scroll on a **release build on a real device**, find the spike, and open the main-thread stack at that timestamp. Profiling a debug build in the simulator is the classic wasted afternoon, because the simulator has your Mac's GPU and no ARC optimisation." },
    { d: 'hard', q: 'What is blending and why does it cost?',
      a: "When a layer is not opaque, the GPU must read what is behind it and combine per pixel, for every overlapping layer. A stack of eight translucent views means eight reads and blends per pixel.\n\nFix: set `isOpaque = true` and give the view a solid `backgroundColor`. A view with a clear background colour is **not opaque** even if it looks solid. Use Color Blended Layers in the simulator's Debug menu or Instruments; green is opaque, red is blended, and a mostly-red screen is a real finding." },
    { d: 'hard', q: 'Explain image decoding as a scroll hazard and give the fix.',
      a: "`UIImage(named:)` and `UIImage(data:)` do not decode immediately. The compressed data is held, and decoding happens **lazily on the main thread the first time the image is drawn**, which is mid-scroll.\n\nFix by forcing the decode in the background at the display size:\n\n```\nlet opts: [CFString: Any] = [\n  kCGImageSourceCreateThumbnailFromImageAlways: true,\n  kCGImageSourceThumbnailMaxPixelSize: maxDim,\n  kCGImageSourceShouldCacheImmediately: true\n]\nlet src = CGImageSourceCreateWithURL(url as CFURL, nil)!\nlet cg = CGImageSourceCreateThumbnailAtIndex(src, 0, opts as CFDictionary)!\n```\n\nThat downsamples and decodes off the main thread in one step, so the main thread only composites. `UIGraphicsImageRenderer` redrawing into a context is the older equivalent." },
    { d: 'medium', q: 'What does `isOpaque` actually do?',
      a: "It tells the compositor that the view covers its bounds completely with fully opaque pixels, so nothing behind it needs to be read or blended. It is a **promise**, not an enforcement: set it true on a view with transparency and you get visual corruption.\n\nIt defaults to true on `UIView` but effectively becomes false the moment `backgroundColor` is nil or has alpha, or `alpha < 1`. `UILabel` with a clear background is the single most common blended layer in a cell." },
    { d: 'hard', q: 'How do you diagnose a hang versus a hitch?',
      a: "A **hitch** is a missed frame during animation, measured in milliseconds. A **hang** is the main thread being unresponsive long enough that the user notices, from about 250ms, and past the watchdog window it becomes a 0x8badf00d termination.\n\nTools differ: Animation Hitches for the former, the **Hangs** instrument or Xcode Organizer's hang reports for the latter. The MetricKit `MXHangDiagnostic` gives you field data. Common causes of hangs specifically: synchronous network on the main thread, a `sync` deadlock, a large synchronous file read, or a main-thread `NSLock` waiting on background work." },
    { d: 'medium', q: 'What is the cost of a deep view hierarchy?',
      a: "Every view costs an object, a backing layer, participation in layout traversal, hit testing traversal, and a compositing pass. A cell with sixty nested views costs on all four axes, repeated for every visible cell every frame.\n\nFlattening helps disproportionately: replace nested containers with constraints on a single level, draw simple decoration with a layer rather than a subview, and use `UILayoutGuide` instead of spacer views. Debug View Hierarchy in Xcode shows the depth, and the view count in a cell is a good quick metric." },
    { d: 'hard', q: 'Why must you profile on a real device with a release build?',
      a: "- The simulator uses your **Mac's** CPU and GPU, which are far faster than the phone's and have a completely different memory architecture.\n- Debug builds disable optimisation, so ARC traffic, generic specialisation and inlining are all absent, often changing timings by several times.\n- Thermal state, Low Power Mode and real storage speed only exist on device.\n\nA performance number from a debug simulator build is not merely imprecise, it can point at the wrong function entirely." },
    { d: 'medium', q: 'What is `CADisplayLink` and when do you use it?',
      a: "A timer synchronised to the display refresh, firing once per frame with a `targetTimestamp` for the upcoming frame. Use it for animation driven by your own state, custom scrubbing, or measuring frame timing.\n\nSet `preferredFrameRateRange` to let the system pick an efficient rate on ProMotion displays rather than always demanding 120Hz. Add it to the run loop in `.common` mode so it keeps firing during scrolling, and always `invalidate()` it, since the run loop retains it exactly like `Timer`." }
  ],
  quiz: [
    { d: 'easy', q: 'The per-frame budget on a 120Hz ProMotion display is:', choices: ['16.7 ms', '8.3 ms', '33 ms', '4 ms'], correct: 1,
      why: 'That budget covers main-thread work, layout, commit and GPU compositing combined.' },
    { d: 'hard', q: 'UIImage(data:) causes scroll jank because decoding happens:', choices: ['On a background queue', 'Lazily on the main thread at first draw', 'During initialisation', 'On the GPU'], correct: 1,
      why: 'Force the decode off the main thread at display size with CGImageSourceCreateThumbnailAtIndex.' },
    { d: 'medium', q: 'A UILabel with a clear background inside a cell is a performance concern because:', choices: ['It allocates a backing store', 'It is non-opaque, so the GPU must blend it against everything behind', 'It breaks Auto Layout', 'It disables reuse'], correct: 1,
      why: 'Set isOpaque true with a solid backgroundColor. Color Blended Layers shows these in red.' },
    { d: 'medium', q: 'Profiling in the simulator with a debug build is misleading mainly because:', choices: ['The simulator has no GPU', 'It uses your Mac CPU/GPU and skips compiler optimisation', 'Instruments does not attach', 'Memory is unlimited'], correct: 1,
      why: 'The differences are large enough to point you at the wrong function, not merely to shift the numbers.' },
    { d: 'hard', q: 'A 300ms freeze on tapping a button is best investigated with:', choices: ['Animation Hitches', 'The Hangs instrument or Xcode Organizer hang reports', 'Leaks', 'Core Animation FPS gauge'], correct: 1,
      why: 'A hang is main-thread unresponsiveness, a different measurement from a missed animation frame.' },
    { d: 'medium', q: 'CADisplayLink must be invalidated because:', choices: ['It is a struct', 'The run loop retains it, exactly like Timer', 'It holds a GPU buffer', 'It blocks the main queue'], correct: 1,
      why: 'Without invalidate() it fires forever and keeps its target alive, which is a classic view controller leak.' }
  ]
});

IPREP.addTopic({
  id: 'battery', domain: 'performance',
  title: 'Battery, Background Execution & Network Efficiency',
  summary: 'What actually drains a phone, and how iOS lets you do work when the app is not open.',
  cards: [
    { d: 'easy', q: 'What are the biggest battery consumers in a typical app?',
      a: "In rough order: the **cellular radio**, **GPS at high accuracy**, the **display** if you keep it awake or drive it at 120Hz unnecessarily, sustained **CPU/GPU** work, and **wake-ups** that prevent the device idling.\n\nThe non-obvious one is wake-ups. Ten small network requests spread over a minute cost far more than one batched request, because each one powers the radio and holds it in a high-power state for several seconds afterwards." },
    { d: 'medium', q: 'What is the radio tail and why does it dominate network energy?',
      a: "After a transfer completes, the cellular radio stays in a high-power state for several seconds before stepping down, in case more data arrives. So a 1KB request can cost as much energy as a 100KB one.\n\nThe consequences: **batch** requests rather than trickling them, **prefetch** what you will plausibly need in the same window, use `URLSession`'s discretionary background transfers so the system can coalesce your traffic with other apps', and never poll on a timer." },
    { d: 'medium', q: 'Compare the background execution modes.',
      a: "- **Background App Refresh** (`BGAppRefreshTask`): short opportunistic wake-ups, seconds, scheduled by the system based on usage patterns. For refreshing content.\n- **Background Processing** (`BGProcessingTask`): minutes, typically overnight while charging. For maintenance, ML training, database cleanup.\n- **Background URLSession**: transfers continue out of process and can relaunch your app on completion. The only reliable way to complete a large download.\n- **Silent push** (`content-available`): server-initiated wake-up, throttled and best-effort.\n- **Declared modes**: audio, location, VoIP, external accessory. Real continuous execution, but App Review requires genuine use.\n\nNone of them is a guarantee. The system decides." },
    { d: 'hard', q: 'What are the practical rules for `BGTaskScheduler`?',
      a: "- Register every task identifier in `Info.plist` **and** in `didFinishLaunching`, before the app finishes launching, or registration throws.\n- Submit the next request when the current task **starts**, not when it finishes, or you can miss a cycle.\n- Set `expirationHandler` and save partial state, because you will be cut off mid-work.\n- Call `setTaskCompleted(success:)` exactly once, or the system penalises your future scheduling.\n- `earliestBeginDate` is a floor, never a promise; the system may run you hours later or not at all.\n\nTest with the `_simulateLaunchForTaskWithIdentifier` debugger command, since waiting for the real scheduler is impractical." },
    { d: 'medium', q: 'How do you use location without destroying the battery?',
      a: "Match the accuracy to the need. `kCLLocationAccuracyBest` runs the GPS chip continuously; `.hundredMeters` or `.kilometer` can use cell and Wi-Fi triangulation, which is dramatically cheaper.\n\nOther levers: `distanceFilter` so you only get callbacks on meaningful movement; **significant location change** monitoring, which is cell-tower based and very cheap and can wake a terminated app; **region monitoring** and beacons for entry and exit events; `allowsBackgroundLocationUpdates` only while genuinely needed; and `pausesLocationUpdatesAutomatically`. Stop updating the instant you no longer need it." },
    { d: 'hard', q: 'What is the watchdog and what are its limits?',
      a: "A system process that terminates apps that block for too long at specific transitions. The exception code is `0x8badf00d` ('ate bad food'). Approximate budgets: about 20 seconds for launch, and around 10 seconds for resuming, suspending or backgrounding.\n\nThe overwhelming cause is synchronous network or disk I/O on the main thread during launch. Note the clock keeps running in the background too, so a slow `applicationDidEnterBackground` can kill you. Xcode Organizer surfaces these as launch hangs from real users." },
    { d: 'medium', q: 'How do you make network usage efficient beyond just batching?',
      a: "- **HTTP caching**: honour `Cache-Control`, send `ETag`/`If-None-Match`, and let a 304 cost you a few bytes instead of a payload.\n- **Compression**: gzip or brotli on responses, which `URLSession` negotiates automatically.\n- **Right-sized assets**: request image variants from the CDN rather than downloading originals.\n- **Delta sync**: ask for changes since a cursor instead of the full list.\n- **`waitsForConnectivity`** instead of failing and retrying on a timer.\n- **`isDiscretionary`** for non-urgent transfers, letting the system pick a good moment such as Wi-Fi while charging." },
    { d: 'hard', q: 'What is thermal throttling and how should an app respond?',
      a: "Sustained CPU or GPU load raises device temperature, and iOS responds by reducing clock speeds, dimming the display and eventually refusing to charge. Read `ProcessInfo.processInfo.thermalState` and observe `thermalStateDidChangeNotification`.\n\nA well-behaved app degrades: at `.serious` reduce frame rate, pause background processing, lower video quality, stop non-essential prefetching. At `.critical` do the minimum. Ignoring it means the system throttles you anyway, but unpredictably and in the middle of something the user cares about." },
    { d: 'medium', q: 'What is Low Power Mode and what should change when it is on?',
      a: "`ProcessInfo.processInfo.isLowPowerModeEnabled`, with `NSProcessInfoPowerStateDidChange` for updates. The system already reduces background refresh, lowers the display refresh rate, and defers discretionary work.\n\nYour app should cooperate visibly: stop autoplaying video, reduce or disable non-essential animation, pause prefetching and polling, lower sync frequency. Apple's own apps do this, and users notice when a third-party app is the one still burning their last 10%." },
    { d: 'hard', q: 'How do you measure energy impact objectively?',
      a: "- **Xcode Energy gauge** while debugging on device: a coarse but immediate signal broken into CPU, network, location and display.\n- **Instruments Energy Log** with the device untethered, then import the trace.\n- **MetricKit**: `MXMetricPayload` delivers daily aggregated CPU time, location time, network bytes and hang time from **real users**, which is the only way to see actual field behaviour.\n- **Xcode Organizer** shows battery and hang metrics across your installed base.\n\nNaming MetricKit specifically signals you have shipped and monitored an app rather than only profiled one locally." }
  ],
  quiz: [
    { d: 'medium', q: 'Ten small requests spread over a minute cost far more energy than one batched request because:', choices: ['TCP handshakes dominate', 'The radio stays in a high-power tail state for seconds after each transfer', 'DNS lookups are cached', 'Each request allocates a thread'], correct: 1,
      why: 'The tail means a 1KB request can cost about the same energy as a 100KB one. Batch and prefetch instead of trickling.' },
    { d: 'hard', q: 'You should submit the next BGAppRefreshTask request:', choices: ['When the current task completes', 'When the current task starts', 'In applicationDidEnterBackground only', 'Once at first launch'], correct: 1,
      why: 'Submitting on start guarantees a pending request exists even if you are expired before finishing.' },
    { d: 'medium', q: 'The 0x8badf00d termination means:', choices: ['Out of memory', 'The watchdog killed you for blocking too long at a lifecycle transition', 'An uncaught exception', 'Invalid code signature'], correct: 1,
      why: 'Roughly 20s for launch and 10s for the other transitions. Synchronous main-thread I/O is the usual cause.' },
    { d: 'medium', q: 'The cheapest location option that can still wake a terminated app is:', choices: ['kCLLocationAccuracyBest with a distanceFilter', 'Significant location change monitoring', 'Continuous background updates', 'requestLocation in a timer'], correct: 1,
      why: 'It is cell-tower based rather than GPS, so it costs very little and still relaunches the app on movement.' },
    { d: 'hard', q: 'At ProcessInfo thermalState .serious a well-behaved app should:', choices: ['Continue as normal, the system handles it', 'Reduce frame rate, pause background processing and prefetching', 'Terminate itself', 'Increase QoS to finish faster'], correct: 1,
      why: 'The system will throttle regardless. Degrading deliberately keeps the reduction away from what the user is actively doing.' },
    { d: 'hard', q: 'The only way to see real-world energy and hang behaviour across your user base is:', choices: ['Instruments Energy Log', 'Xcode Energy gauge', 'MetricKit payloads and Xcode Organizer', 'Console logs'], correct: 2,
      why: 'The first two are local profiling. MetricKit delivers aggregated field metrics from actual devices.' }
  ]
});
