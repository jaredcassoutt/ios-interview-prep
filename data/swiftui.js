/* SwiftUI & Declarative UI — 8 topics */

IPREP.addTopic({
  id: 'swiftui-identity', domain: 'swiftui',
  title: 'View Identity, Diffing & the Render Loop',
  summary: 'What a View actually is, how SwiftUI decides to redraw, and why identity governs everything.',
  cards: [
    { d: 'easy', q: 'What is a SwiftUI `View`, really?',
      a: "A **value type describing what the UI should look like** for a given state, not an object that persists on screen. Your struct is created and thrown away constantly.\n\nThe persistent thing is the **render tree** SwiftUI maintains behind it, plus the state attached to each node. So a `View` struct is closer to a recipe than to a `UIView`. This single fact explains why `body` can run dozens of times, why `@State` lives outside the struct, and why side effects in `body` are a bug." },
    { d: 'medium', q: 'How often does `body` run, and what follows from that?',
      a: "Whenever any dependency SwiftUI is tracking for that view changes, and sometimes more often than you expect. It is cheap by design and you must treat it as such.\n\nConsequences:\n- **No side effects in `body`.** No network calls, no analytics, no mutating state, no allocating expensive objects.\n- **No `Date()` or `UUID()`** in `body` unless you want a different value each pass.\n- Expensive computation belongs in the model or a cached property, not in `body`.\n\nUse `.task`, `.onAppear` or `.onChange` for side effects." },
    { d: 'hard', q: 'Explain structural identity versus explicit identity.',
      a: "**Structural identity** is inferred from a view's position in the view tree. Two branches of an `if/else` are different identities even if they produce the same view type.\n\n**Explicit identity** is what you assign with `.id(value)` or via `ForEach`'s identifier.\n\nIdentity is the thing SwiftUI uses to decide 'is this the same view as last time, or a new one?' That decision determines whether state is preserved, and whether a change animates or replaces." },
    { d: 'hard', q: 'Why does `@State` reset unexpectedly, and how does identity cause it?',
      a: "State is attached to a view's **identity**, not to the struct. Change the identity and SwiftUI discards the old state and creates fresh state.\n\nThe common triggers:\n- An `if/else` swapping branches, since each branch is a distinct identity.\n- `.id()` bound to a value that changes.\n- A `ForEach` whose identifiers change because you keyed on content rather than a stable id.\n\nThe fix is nearly always to give the view a stable identity, or to move the state up to an owner whose identity is stable." },
    { d: 'medium', q: 'What does `.id()` do, and when does it hurt?',
      a: "It assigns explicit identity. Changing it tells SwiftUI 'this is a different view now', which destroys the old state and view, creates a new one, and runs the transition rather than an animation.\n\nUseful for deliberately resetting a subtree, for example clearing a form. Harmful when applied to something that changes often, because you throw away state and force a teardown every time. A `.id(UUID())` in `body` is the classic disaster: new identity on every render, so nothing is ever reused." },
    { d: 'hard', q: 'Why is `AnyView` discouraged?',
      a: "It **erases the static type**, which is exactly what SwiftUI uses to diff efficiently. With a concrete generic type, SwiftUI can compare old and new structurally and update only what changed. Inside `AnyView` it cannot see the shape, so it tends to tear down and rebuild the subtree.\n\nAlternatives: `@ViewBuilder` on a function or computed property, `Group` with `if/else`, or making the branches a single type with different data. Reach for `AnyView` only when you genuinely need heterogeneous views in an array." },
    { d: 'medium', q: 'What does `@ViewBuilder` do?',
      a: "It is a result builder that turns a block of view expressions into a single composed view, supporting `if`, `switch`, and up to ten statements via `TupleView`.\n\nIt is why you can write conditionals inside a `VStack` without returning explicitly. Apply it to your own functions and computed properties to get the same ergonomics without `AnyView`:\n\n```\n@ViewBuilder\nvar content: some View {\n    if isLoading { ProgressView() }\n    else { list }\n}\n```" },
    { d: 'hard', q: 'How does SwiftUI know which views to re-render?',
      a: "It records **dependencies** as `body` runs. Reading `@State`, a `@Binding`, an `@Environment` value, or a tracked property of an `@Observable` object registers that view as a dependent. When the value changes, only the views that read it are invalidated.\n\nWith the older `ObservableObject`, invalidation is coarser: `objectWillChange` fires for the whole object, so every view observing it re-renders even if it read an unrelated property. That difference is the main performance argument for the Observation framework." },
    { d: 'medium', q: 'How do you actually debug unnecessary re-renders?',
      a: "`Self._printChanges()` inside `body` prints which dependency triggered that pass:\n\n```\nvar body: some View {\n    let _ = Self._printChanges()\n    ...\n}\n```\n\nIt tells you whether it was `@self`, a specific property, or `@identity`. `@identity` means the view was recreated rather than updated, which is usually an identity bug. Instruments also has a SwiftUI template showing view body counts per update." },
    { d: 'hard', q: 'What is `EquatableView` / `.equatable()` for?',
      a: "It tells SwiftUI to compare the view's stored properties with `==` and skip re-running `body` when they are unchanged. By default SwiftUI does its own comparison, which can be conservative for types it cannot cheaply compare, notably closures.\n\nUse it when a view is expensive to build and you can prove equality cheaply. It is a targeted optimisation, not a default: a bad `==` that ignores a real dependency gives you stale UI, which is worse than a redundant render." },
    { d: 'medium', q: 'Why does modifier order matter?',
      a: "Each modifier **wraps** the view it is applied to and returns a new view, so order is composition order.\n\n`.padding().background(.red)` pads first, then paints the background behind the padded view, giving a red area including the padding. `.background(.red).padding()` paints the background tight to the content, then pads outside it.\n\nThe mental model is that you are building a nested tree from the inside out, not setting properties on an object." }
  ],
  quiz: [
    { d: 'medium', q: 'A SwiftUI View struct is best described as:', choices: ['A long-lived object like UIView', 'A value-type description of the UI for a given state', 'A view controller replacement', 'A layer-backed renderer'], correct: 1,
      why: 'The struct is created and discarded constantly. SwiftUI keeps a separate render tree, which is why state lives outside the struct.' },
    { d: 'hard', q: 'Your @State resets whenever a toggle flips. Most likely cause:', choices: ['@State cannot hold that type', 'An if/else swap changes the structural identity, so state is discarded', 'You need @StateObject instead', 'The view is not Equatable'], correct: 1,
      why: 'Each branch of an if/else is a separate identity. Give the view stable identity, or lift the state to a stable owner.' },
    { d: 'medium', q: '`.id(UUID())` written inside body causes:', choices: ['Better animation', 'A brand-new identity every render, so nothing is ever reused', 'Nothing, it is ignored', 'A compile error'], correct: 1,
      why: 'The view and all its state are destroyed and rebuilt on every pass. It is the classic accidental performance and state-loss bug.' },
    { d: 'hard', q: 'AnyView hurts performance because it:', choices: ['Allocates on the heap only', 'Erases the static type SwiftUI uses to diff, so subtrees get rebuilt', 'Disables animations', 'Forces main-thread layout'], correct: 1,
      why: 'Prefer @ViewBuilder or Group with if/else, which keep the concrete type visible to the diffing algorithm.' },
    { d: 'medium', q: '`Self._printChanges()` reporting `@identity` means:', choices: ['A property changed', 'The view was recreated rather than updated', 'The environment changed', 'A binding fired'], correct: 1,
      why: 'That points to an identity bug: something upstream is changing the view identity instead of just its data.' },
    { d: 'easy', q: '`.padding().background(.red)` versus `.background(.red).padding()`:', choices: ['Identical output', 'The first paints red including the padding; the second paints red tight to the content', 'The second is invalid', 'Only the first animates'], correct: 1,
      why: 'Modifiers wrap the view they are applied to, so order is composition order, not property assignment.' }
  ]
});

IPREP.addTopic({
  id: 'swiftui-state', domain: 'swiftui',
  title: 'State & Data Flow',
  summary: 'The property wrappers, which one owns the truth, and the bugs each causes.',
  cards: [
    { d: 'easy', q: 'Name each state property wrapper and what it owns.',
      a: "- `@State`: this view **owns** a piece of value-type state. Mark it `private`.\n- `@Binding`: a two-way reference to state **owned elsewhere**. Owns nothing.\n- `@StateObject`: this view **owns** a reference-type object and controls its lifetime.\n- `@ObservedObject`: observes an object **owned elsewhere**. Owns nothing.\n- `@EnvironmentObject`: pulls an object from the environment by type.\n- `@Environment`: reads a system or custom environment value.\n\nThe whole system reduces to one question: who owns this, and who is just reading it?" },
    { d: 'hard', q: 'What is the difference between `@StateObject` and `@ObservedObject`, and what breaks?',
      a: "`@StateObject` initialises its object **exactly once** for the life of that view identity. `@ObservedObject` does not manage lifetime at all: it just observes whatever it is handed.\n\nThe bug: writing `@ObservedObject var vm = ViewModel()` recreates the view model **every time the parent re-renders**, because the struct is recreated and the initialiser runs again. State resets, in-flight work restarts, and it looks like random data loss.\n\nRule: `@StateObject` where you create it, `@ObservedObject` where you receive it." },
    { d: 'medium', q: 'When is `@StateObject`\'s initialiser actually called?',
      a: "Lazily, on first `body` evaluation for that view identity, and never again while the identity is stable. Note the autoclosure: `@StateObject var vm = ViewModel()` does not run `ViewModel()` on every struct init, it is stored as a closure and invoked once.\n\nThis is also why injecting into a `@StateObject` is awkward. If you need constructor arguments, use `init(...)` with `_vm = StateObject(wrappedValue: ViewModel(id: id))`, and understand that the wrapped value expression still only runs once." },
    { d: 'medium', q: 'What is `@Binding` and how do you create one?',
      a: "A read-write reference to state someone else owns. You make one with the `$` projection: `TextField(\"Name\", text: $name)` passes a `Binding<String>` into the field.\n\nYou can also construct one manually with `Binding(get:set:)`, which is the escape hatch for adapting a value that is not directly stored, and `.constant(value)` for previews and tests. Use it whenever a child needs to write back, and pass the plain value instead when the child only reads." },
    { d: 'hard', q: 'What are the trade-offs of `@EnvironmentObject`?',
      a: "It removes the need to thread a dependency through every intermediate view, which is genuinely useful for app-wide things like a session or a theme.\n\nThe costs are real:\n- **Runtime crash** if no object of that type was injected. There is no compile-time check.\n- Dependencies become invisible, so you cannot read a view's requirements from its signature.\n- It invites a single fat object that every view observes, which widens invalidation.\n\nUse it for a small number of genuinely global objects, and inject everything else explicitly." },
    { d: 'medium', q: 'What is `@Environment` for, beyond system values?',
      a: "It reads values from the environment: `\\.colorScheme`, `\\.dismiss`, `\\.dynamicTypeSize`, `\\.scenePhase`, `\\.isEnabled` and so on.\n\nYou can define your own with an `EnvironmentKey`, which is the idiomatic way to pass configuration down a subtree without a global. Since iOS 17 it also reads `@Observable` objects by type, replacing much of what `@EnvironmentObject` did but without the separate wrapper." },
    { d: 'medium', q: 'State the single-source-of-truth rule and why it matters.',
      a: "Every piece of state should have exactly one owner, and everyone else derives from it or holds a binding to it.\n\nDuplicating state, for example copying a model value into `@State` in `onAppear`, creates two truths that drift. When they disagree the UI shows one and the model has the other, and the bug appears only after a specific sequence of edits.\n\nWhen you feel the urge to copy, ask instead: can this be computed from the source? Derived values should be computed properties, not stored state." },
    { d: 'hard', q: 'Where should state live when two sibling views need it?',
      a: "In their nearest common ancestor, passed down as a `@Binding` to whichever child mutates it. This is state hoisting, the same principle as React.\n\nIf that ancestor is many levels up and the plumbing gets painful, that is the signal to reach for an observable model object held by the ancestor and injected, rather than to duplicate state in both siblings and try to sync them." },
    { d: 'medium', q: 'What do `@AppStorage` and `@SceneStorage` do?',
      a: "`@AppStorage` is a property wrapper over `UserDefaults` that invalidates dependent views when the value changes: `@AppStorage(\"hasOnboarded\") var hasOnboarded = false`. It is for genuine user preferences, and the `UserDefaults` caveats still apply, so no secrets and nothing large.\n\n`@SceneStorage` persists small per-scene UI state for state restoration, such as the selected tab or a scroll position. It is scoped to the scene and managed by the system." },
    { d: 'hard', q: 'You mutate an @Observable model from a background task and the UI does not update. Why?',
      a: "Almost always a threading issue rather than an observation issue. SwiftUI expects state mutation on the main actor, and mutating from a background context gives you undefined behaviour, missed updates, or a crash.\n\nMark the model or its mutating methods `@MainActor`, or hop explicitly before assigning. With `async/await` the clean answer is to annotate the model `@MainActor` and let the compiler enforce it, rather than remembering to dispatch at each call site." },
    { d: 'medium', q: 'Why should `@State` be `private`?',
      a: "Because it is the view's own storage and nobody outside should set it. Making it non-private lets a parent pass an initial value through the memberwise initialiser, which looks like it works and then silently does nothing on later updates, since `@State` initialisers only run once per identity.\n\nIf a parent needs to supply the value, that state does not belong in this view. Hoist it and pass a `@Binding`." }
  ],
  quiz: [
    { d: 'hard', q: '`@ObservedObject var vm = ViewModel()` written inside a view causes:', choices: ['Nothing unusual', 'The view model to be recreated on every parent re-render, resetting state', 'A compile error', 'A retain cycle'], correct: 1,
      why: 'ObservedObject does not manage lifetime. Use @StateObject where the object is created, @ObservedObject where it is received.' },
    { d: 'medium', q: '@StateObject initialises its object:', choices: ['On every body pass', 'Once per view identity', 'Once per app launch', 'Every time the parent updates'], correct: 1,
      why: 'The wrapped value is an autoclosure evaluated lazily on first body and retained for that identity.' },
    { d: 'medium', q: 'The main risk of @EnvironmentObject is:', choices: ['Poor performance', 'A runtime crash when nothing of that type was injected', 'It cannot hold classes', 'It only works on iOS 17'], correct: 1,
      why: 'There is no compile-time check, and it also hides a view dependency that its signature no longer documents.' },
    { d: 'medium', q: 'Two sibling views need to share state. It should live:', choices: ['Duplicated in both, synced with onChange', 'In their nearest common ancestor, passed down as a Binding', 'In a global singleton', 'In @AppStorage'], correct: 1,
      why: 'State hoisting keeps a single source of truth. Duplicating creates two truths that drift apart.' },
    { d: 'hard', q: 'Copying a model value into @State in onAppear is a smell because:', choices: ['onAppear is too late', 'It creates a second source of truth that drifts from the model', '@State cannot hold model types', 'It runs twice'], correct: 1,
      why: 'Derived values should be computed from the source, not stored separately and manually kept in sync.' },
    { d: 'easy', q: '`$value` on a @State property gives you:', choices: ['The wrapped value', 'A Binding to it', 'A copy', 'An ObservableObject'], correct: 1,
      why: 'The dollar prefix accesses the projected value, which for @State is a Binding the child can write through.' }
  ]
});

IPREP.addTopic({
  id: 'swiftui-observation', domain: 'swiftui',
  title: 'Observation: @Observable vs ObservableObject',
  summary: 'The iOS 17 Observation framework, why it is faster, and how to migrate.',
  cards: [
    { d: 'easy', q: 'What does the `@Observable` macro replace?',
      a: "It replaces the `ObservableObject` protocol with `@Published` properties. Instead of conforming and annotating each property, you mark the class `@Observable` and every stored property becomes observable automatically.\n\nIn the view you then just use `@State` for ownership, or plain `let` for a passed-in reference. `@ObservedObject`, `@StateObject` and `@EnvironmentObject` all become unnecessary for these types." },
    { d: 'hard', q: 'Why is `@Observable` more efficient than `ObservableObject`?',
      a: "`ObservableObject` publishes a single `objectWillChange` signal for the **whole object**. Every view observing that object is invalidated when any property changes, even one it never reads.\n\n`@Observable` tracks access **per property**. SwiftUI records exactly which properties a view read during `body`, and only invalidates views that read the property that changed. On a screen with one large model and many small views, this is the difference between redrawing everything and redrawing one label." },
    { d: 'medium', q: 'How do you own an `@Observable` object in a view?',
      a: "```\n@Observable final class ProfileModel { var name = \"\" }\n\nstruct ProfileView: View {\n    @State private var model = ProfileModel()\n    var body: some View { Text(model.name) }\n}\n```\n\n`@State` now handles reference types too, giving the same once-per-identity lifetime that `@StateObject` used to provide. A child that merely receives the object takes it as a plain `let`, with no wrapper at all." },
    { d: 'medium', q: 'What is `@Bindable` for?',
      a: "It creates bindings to the properties of an `@Observable` object:\n\n```\nstruct EditView: View {\n    @Bindable var model: ProfileModel\n    var body: some View { TextField(\"Name\", text: $model.name) }\n}\n```\n\nA plain `let model` is enough to **read**, but you need `@Bindable` to get the `$` projection for two-way controls. You can also use it inline: `@Bindable var m = model` inside `body`." },
    { d: 'hard', q: 'How does observation tracking actually work?',
      a: "The macro rewrites stored properties into computed properties that call `access(keyPath:)` on read and `withMutation(keyPath:)` on write, backed by an `ObservationRegistrar`.\n\nSwiftUI wraps each `body` evaluation in `withObservationTracking`, which records the key paths read and registers a callback for the next change to any of them. That is why the dependency set is exact and why it is recomputed on every render rather than declared up front." },
    { d: 'medium', q: 'How do you put an `@Observable` object in the environment?',
      a: "```\n.environment(session)              // inject\n@Environment(Session.self) var session   // read\n```\n\nNote the read is optional-typed if you write `@Environment(Session.self) var session: Session?`. Using the non-optional form still traps at runtime when nothing was injected, so the `@EnvironmentObject` crash risk has not disappeared, it has only moved." },
    { d: 'hard', q: 'What are the migration gotchas from ObservableObject?',
      a: "- Remove `@Published`; the macro handles all stored properties, and leaving it causes a compile error.\n- `@StateObject` becomes `@State`, `@ObservedObject` becomes a plain `let`, `@EnvironmentObject` becomes `@Environment(Type.self)`.\n- `objectWillChange` no longer exists, so any Combine pipeline hanging off it must be rewritten.\n- A computed property is only observable if the stored properties it reads are, which is usually fine but surprises people using lazily cached values.\n- It raises the deployment target to iOS 17." },
    { d: 'medium', q: 'When should you still use `ObservableObject`?',
      a: "When you support iOS 16 or earlier, or when you have an existing Combine pipeline built on `objectWillChange` and the rewrite is not worth it. `ObservableObject` is not deprecated and works fine.\n\nFor anything new on a modern deployment target, `@Observable` is the default: less boilerplate, better invalidation, and it works with plain `let` in child views." },
    { d: 'hard', q: 'Does `@Observable` solve threading for you?',
      a: "No. It makes observation efficient; it says nothing about which thread mutates the state. Mutating an observable model off the main actor is still a bug and still produces missed or corrupted UI updates.\n\nThe combination you want is `@Observable @MainActor final class Model`, so the compiler enforces main-actor mutation and SwiftUI's expectations hold. Then any background work happens in `async` functions that hop back before assigning." },
    { d: 'medium', q: 'How do you test an `@Observable` model?',
      a: "Exactly like a plain object, which is the point. Instantiate it, call methods, assert on properties. There is no publisher to await and no view needed.\n\nIf you want to assert that a change fires observation, wrap a read in `withObservationTracking(_:onChange:)` and assert the callback runs. In practice most tests just check the resulting state, which is simpler and less brittle." }
  ],
  quiz: [
    { d: 'hard', q: '@Observable outperforms ObservableObject mainly because it:', choices: ['Uses structs instead of classes', 'Tracks reads per property instead of publishing one signal for the whole object', 'Avoids Combine', 'Runs off the main thread'], correct: 1,
      why: 'objectWillChange invalidates every observing view. Per-property tracking invalidates only the views that read the changed property.' },
    { d: 'medium', q: 'To own an @Observable object in a view you now use:', choices: ['@StateObject', '@State', '@ObservedObject', '@Bindable'], correct: 1,
      why: '@State handles reference types too and gives the once-per-identity lifetime @StateObject used to provide.' },
    { d: 'medium', q: '@Bindable is needed when you want to:', choices: ['Read a property', 'Get $ bindings to an @Observable object for two-way controls', 'Inject into the environment', 'Observe on a background thread'], correct: 1,
      why: 'A plain let is enough to read. The projection for TextField and friends requires @Bindable.' },
    { d: 'hard', q: 'Leaving @Published on a property inside an @Observable class:', choices: ['Is required', 'Causes a compile error', 'Silently does nothing', 'Improves performance'], correct: 1,
      why: 'The macro owns all stored properties. @Published belongs only to ObservableObject.' },
    { d: 'medium', q: '@Observable handles main-thread safety for you:', choices: ['True', 'False, you still need @MainActor on the model'], correct: 1,
      why: 'Observation is about invalidation, not isolation. Mutating off the main actor is still a bug.' }
  ]
});

IPREP.addTopic({
  id: 'swiftui-layout', domain: 'swiftui',
  title: 'The SwiftUI Layout System',
  summary: 'Proposal and response, stacks, frames, GeometryReader, and the custom Layout protocol.',
  cards: [
    { d: 'easy', q: 'Describe the SwiftUI layout algorithm in three steps.',
      a: "1. **The parent proposes a size** to the child. It may propose `nil` in a dimension, meaning 'tell me your ideal'.\n2. **The child chooses its own size.** It is not forced; a `Text` takes what it needs, an `Image` may ignore the proposal entirely.\n3. **The parent places the child** in its own coordinate space.\n\nThe key insight, and the thing candidates miss, is that **the child decides**. A parent cannot impose a size, it can only propose one. That is why `.frame()` does not resize a child so much as create a new parent that proposes differently." },
    { d: 'medium', q: 'What does `.frame(width:height:)` actually do?',
      a: "It creates a **new view** that proposes the given size to its child and is itself that size. It does not set a property on the child.\n\nSo if the child refuses the proposal, for example a `Text` with unbreakable content, the child overflows the frame rather than shrinking. And `.frame(maxWidth: .infinity)` means 'propose as much as I am offered', which is how you make a view fill available width. This distinction explains most 'why is my frame being ignored' questions." },
    { d: 'hard', q: 'Why is `GeometryReader` a trap, and what should you use instead?',
      a: "`GeometryReader` **accepts the entire proposed size** and its content is placed top-leading by default, so dropping one into a stack often collapses or expands the layout unexpectedly. It also reads a size that is only known during layout, which invites reading-then-writing state and a layout loop.\n\nModern alternatives:\n- `ViewThatFits` to pick among candidates.\n- `.containerRelativeFrame()` for a fraction of the container.\n- `onGeometryChange` (iOS 18) or a preference key to read a size without restructuring layout.\n- A custom `Layout` when you genuinely need to measure and place." },
    { d: 'medium', q: 'What is `fixedSize()` for?',
      a: "It tells a view to use its **ideal** size in the given dimension, ignoring the proposal. The canonical use is a `Text` being truncated because the parent proposed too little width: `.fixedSize(horizontal: false, vertical: true)` lets it grow vertically to fit rather than clipping.\n\nUse it deliberately and narrowly. Applying it in both dimensions to something large lets the view escape its container entirely." },
    { d: 'hard', q: 'How do stacks distribute space among children?',
      a: "A stack proposes to its children in order of **layout priority**, then by flexibility. It asks the least flexible children first, gives them what they need, and divides the remainder among the more flexible ones.\n\nThat is why a `Text` next to a `Spacer` keeps its natural width: `Text` is inflexible, `Spacer` takes everything left. To override the order, set `.layoutPriority()`; a higher priority child is served first. Understanding this ordering is what lets you fix 'the wrong view is being squashed'." },
    { d: 'medium', q: 'What are alignment guides and when do you need one?',
      a: "A stack aligns children on a named guide, `.leading`, `.center`, `.firstTextBaseline` and so on. `alignmentGuide(_:computeValue:)` lets a specific child redefine where its guide sits.\n\nThe real use case is aligning views across different containers, for example lining up a label in an `HStack` with a label in another row. For that you define a **custom alignment** with an `AlignmentID` and set it on both, which is the answer interviewers listen for." },
    { d: 'hard', q: 'What is the `Layout` protocol and when would you implement one?',
      a: "Introduced in iOS 16, it lets you write a real layout container by implementing `sizeThatFits(proposal:subviews:cache:)` and `placeSubviews(in:proposal:subviews:cache:)`.\n\nUse it for genuinely custom arrangements a stack cannot express: a flow layout that wraps, a radial arrangement, a masonry grid. It is far better than nesting `GeometryReader` and manual offsets, because it participates properly in the proposal system, animates, and can cache measurements." },
    { d: 'medium', q: 'How do `LazyVGrid` and `Grid` differ?',
      a: "`LazyVGrid`/`LazyHGrid` take `GridItem` column definitions and lay children out lazily, creating only what is visible. Good for large collections.\n\n`Grid` (iOS 16) is an eager, table-like container with real rows and columns, cell spanning via `gridCellColumns`, and alignment across rows. Good for forms and small structured layouts where you want columns to line up. Choosing `Grid` for a thousand items is a performance bug; choosing `LazyVGrid` for a six-cell form is awkward." },
    { d: 'medium', q: 'How does safe area work in SwiftUI?',
      a: "Layout is inset to the safe area by default. `.ignoresSafeArea(_:edges:)` opts a view out, which you want for a full-bleed background but almost never for content.\n\n`.safeAreaInset(edge:)` adds your own view to the safe area, which is how you place a persistent bottom bar that content correctly insets around, rather than overlaying it and having the last row hidden. `.safeAreaPadding` applies padding matching the safe area without changing the layout ownership." },
    { d: 'hard', q: 'Your layout jumps or loops. What are the usual causes?',
      a: "- Reading a geometry value and writing it into `@State`, which invalidates layout, which recomputes the value, which writes again.\n- A preference key whose value changes on every pass.\n- `GeometryReader` nested inside something that sizes to its content, creating a circular dependency.\n- An `.id()` changing during layout.\n\nThe fix is to break the cycle: use `onGeometryChange` or a preference with a stable comparison, or restructure so the size flows one way. If you must store a measured size, guard the write so equal values do not trigger an update." }
  ],
  quiz: [
    { d: 'medium', q: 'In SwiftUI layout, the size of a view is ultimately decided by:', choices: ['The parent', 'The child, responding to a proposal', 'The frame modifier', 'The root window'], correct: 1,
      why: 'Parents propose, children choose, parents place. That is why a child can overflow a frame it refuses.' },
    { d: 'hard', q: 'Dropping a GeometryReader into a VStack often breaks layout because it:', choices: ['Renders asynchronously', 'Accepts the entire proposed size and places content top-leading', 'Cannot be nested', 'Disables animations'], correct: 1,
      why: 'It is greedy. Prefer ViewThatFits, containerRelativeFrame, or onGeometryChange when you only need to measure.' },
    { d: 'medium', q: 'A Text is truncating even though there is vertical room. The fix is:', choices: ['.frame(height: 100)', '.fixedSize(horizontal: false, vertical: true)', '.lineLimit(1)', '.scaledToFit()'], correct: 1,
      why: 'It tells the text to take its ideal height rather than accepting a proposal that forces truncation.' },
    { d: 'hard', q: 'For a wrapping flow layout of tags, the right tool is:', choices: ['Nested GeometryReaders with offsets', 'A custom Layout conformance', 'LazyVGrid with fixed columns', 'HStack with Spacers'], correct: 1,
      why: 'Layout participates in the proposal system, animates correctly, and can cache measurements. The GeometryReader approach fights the framework.' },
    { d: 'medium', q: 'To add a persistent bottom bar that content insets around, use:', choices: ['.overlay(alignment: .bottom)', '.safeAreaInset(edge: .bottom)', '.ignoresSafeArea()', 'ZStack'], correct: 1,
      why: 'An overlay covers content, so the last row hides behind the bar. safeAreaInset extends the safe area properly.' },
    { d: 'hard', q: 'A layout loop most often comes from:', choices: ['Too many modifiers', 'Writing a measured geometry value back into state that re-triggers layout', 'Using LazyVStack', 'Animating a frame'], correct: 1,
      why: 'Break the cycle, or guard the write so an unchanged value does not trigger another pass.' }
  ]
});

IPREP.addTopic({
  id: 'swiftui-lists', domain: 'swiftui',
  title: 'Lists, ForEach & Scroll Performance',
  summary: 'List vs lazy stacks, identity in ForEach, and how to keep a feed at 120fps.',
  cards: [
    { d: 'medium', q: 'When do you use `List` versus `LazyVStack` in a `ScrollView`?',
      a: "`List` is backed by `UICollectionView` on iOS, so you get **real cell reuse**, built-in row separators, swipe actions, selection, editing and section headers. It is the right default for anything list-shaped.\n\n`LazyVStack` in a `ScrollView` gives you full layout freedom and lazily creates rows, but **it does not reuse views**: rows that scroll off are discarded and rebuilt, and everything created stays in the layout. Choose it when you need a layout `List` cannot express, and expect to do more performance work yourself." },
    { d: 'hard', q: 'Why does `ForEach` identity matter so much?',
      a: "`ForEach` needs a stable identity per element to know what was inserted, removed or moved. Get it wrong and you see wrong animations, lost `@State` inside rows, wrong data on rows after an update, and scroll position jumps.\n\n`ForEach(items)` requires `Identifiable`. `ForEach(items, id: \\.self)` hashes the **whole element**, so any content change makes it a different identity and the row is destroyed and recreated. That is the single most common SwiftUI list bug, and it is exactly the diffable-data-source identity issue in a new syntax." },
    { d: 'medium', q: 'What does `id: \\.self` actually do, and when is it acceptable?',
      a: "It uses the element itself as the identifier, requiring `Hashable`. Acceptable for genuinely immutable, unique values such as a fixed array of enum cases or strings you control.\n\nUnacceptable for model objects, because editing any property changes the hash and therefore the identity. It is also silently wrong for arrays containing duplicates, since two equal elements collide into one identity and SwiftUI drops or duplicates rows." },
    { d: 'medium', q: 'How do you load data for a view correctly?',
      a: "`.task { }` is the modern answer. It runs when the view appears, and its `Task` is **automatically cancelled when the view disappears**, which `onAppear` plus a manual `Task` does not give you.\n\n`.task(id:)` re-runs when the id changes, which is exactly what you want for a detail view whose subject changes. Use `.onAppear` only for genuinely synchronous setup, and remember it can fire more than once in a lazy container." },
    { d: 'hard', q: 'How do you implement infinite scroll in SwiftUI?',
      a: "Attach a `.task` or `.onAppear` to the last row, or to a sentinel view near the end, and trigger the next page from there:\n\n```\nForEach(items) { item in\n    Row(item)\n        .task {\n            if item == items.last { await store.loadNextPage() }\n        }\n}\n```\n\nThe store must dedupe concurrent page loads and handle a refresh landing mid-load, exactly as in UIKit. The SwiftUI-specific trap is that in a lazy container this can fire more than once, so the guard in the store is doing real work." },
    { d: 'medium', q: 'How do you control or read scroll position?',
      a: "- `ScrollViewReader` with `scrollTo(_:anchor:)` scrolls to a view by id. Works everywhere but is imperative.\n- `.scrollPosition(id:)` (iOS 17) binds the top-most visible item id, giving two-way control.\n- `.scrollTargetBehavior(.paging)` and `.scrollTargetLayout()` give paging and snapping.\n- `.defaultScrollAnchor(.bottom)` starts at the bottom, which is what a chat wants.\n\nBefore iOS 17 the honest answer is that fine scroll control was a real reason to drop to UIKit." },
    { d: 'hard', q: 'What actually makes a SwiftUI list slow?',
      a: "- Expensive work in `body`: formatting dates, decoding, sorting, filtering a large array on every pass.\n- `AnyView` in rows, defeating diffing.\n- A single large `ObservableObject` so every row invalidates on any change.\n- Unstable `ForEach` identity forcing teardown and rebuild.\n- `LazyVStack` where `List` would reuse.\n- Loading full-resolution images without downsampling, exactly as in UIKit.\n\nMeasure with the SwiftUI Instruments template and `Self._printChanges()` before optimising." },
    { d: 'medium', q: 'How do you get pull to refresh and swipe actions?',
      a: "`.refreshable { await store.refresh() }` on a `List` or `ScrollView` gives you the system pull-to-refresh, and it is `async`, so the spinner stays until your work completes.\n\n`.swipeActions(edge:)` on a row adds leading or trailing actions, with `allowsFullSwipe` for the full-swipe gesture. Both are `List` features you would otherwise rebuild by hand in a `LazyVStack`, which is a good argument for `List`." },
    { d: 'hard', q: 'How do sections and pinned headers work?',
      a: "In a `List`, `Section { } header: { }` gives you grouped rows with the platform's section behaviour. In a `LazyVStack` you get pinning by passing `pinnedViews: [.sectionHeaders]` and using `Section`.\n\nThe performance note: pinned headers in a lazy stack keep the header view alive while its section is on screen, which is cheap, but a header that observes a large model will re-render as you scroll. Keep headers dumb." },
    { d: 'medium', q: 'How do you animate list changes correctly?',
      a: "Wrap the data mutation in `withAnimation`, and make sure the identity is stable so SwiftUI can see the change as an insert or move rather than a wholesale replacement.\n\n```\nwithAnimation { items.remove(atOffsets: offsets) }\n```\n\nIf every row animates when one changes, your identity is content-based rather than identity-based. That is the same root cause as the diffable data source problem in UIKit." }
  ],
  quiz: [
    { d: 'medium', q: 'The key advantage of List over LazyVStack in a ScrollView is:', choices: ['It supports more modifiers', 'Real cell reuse, plus separators, swipe actions and selection', 'It is not lazy', 'It renders on a background thread'], correct: 1,
      why: 'LazyVStack creates lazily but does not reuse, so rows are rebuilt rather than recycled.' },
    { d: 'hard', q: '`ForEach(posts, id: \\.self)` on a mutable model type causes:', choices: ['Better performance', 'Rows to be destroyed and recreated whenever any property changes', 'A compile error', 'Duplicate keys to be merged safely'], correct: 1,
      why: 'Hashing the whole element makes identity content-based. Use a stable id, exactly as with a diffable data source.' },
    { d: 'medium', q: '`.task { }` is preferred over `.onAppear` for async work because it:', choices: ['Runs earlier', 'Cancels its Task automatically when the view disappears', 'Runs on a background thread', 'Only fires once ever'], correct: 1,
      why: 'It ties the task lifetime to the view lifetime. .task(id:) also re-runs when the id changes.' },
    { d: 'medium', q: 'Pull to refresh in SwiftUI is:', choices: ['.onRefresh', '.refreshable { await ... }', '.pullToRefresh()', 'A UIViewRepresentable'], correct: 1,
      why: 'It is async, so the spinner remains until the awaited work finishes.' },
    { d: 'hard', q: 'Every row re-animates when one item changes. The likely cause is:', choices: ['withAnimation was omitted', 'Identity is content-based, so SwiftUI sees a wholesale replacement', 'The list is too long', 'You used List instead of LazyVStack'], correct: 1,
      why: 'Stable identity lets the diff express one insert, delete or move instead of replacing the collection.' },
    { d: 'medium', q: 'To bind the top-most visible item id on iOS 17 you use:', choices: ['ScrollViewReader', '.scrollPosition(id:)', 'GeometryReader', '.onScroll'], correct: 1,
      why: 'It gives two-way control. ScrollViewReader still works but is imperative and one-way.' }
  ]
});

IPREP.addTopic({
  id: 'swiftui-navigation', domain: 'swiftui',
  title: 'Navigation, Sheets & Deep Linking',
  summary: 'NavigationStack, typed paths, programmatic routing, and the modal presentation rules.',
  cards: [
    { d: 'easy', q: 'What did `NavigationStack` fix that `NavigationView` could not do?',
      a: "`NavigationView` had no real programmatic control. You pushed by flipping `isActive` bindings on individual links, which did not compose, could not express a deep stack, and made deep linking painful.\n\n`NavigationStack` takes a `path` binding representing the whole stack as data. You can push, pop, replace and restore by mutating an array, which makes deep links, state restoration and 'go back three screens' all trivial." },
    { d: 'medium', q: 'How do `path` and `navigationDestination` fit together?',
      a: "```\nNavigationStack(path: $path) {\n    RootView()\n        .navigationDestination(for: Product.self) { ProductView(product: $0) }\n        .navigationDestination(for: Order.self)   { OrderView(order: $0) }\n}\n```\n\nYou append a value to `path` and SwiftUI finds the destination registered for that **type**. `NavigationLink(value:)` does the same thing declaratively. The stack is data; the destinations are a type-to-view mapping." },
    { d: 'hard', q: 'What is `NavigationPath` and when do you need it over a typed array?',
      a: "A type-erased stack that can hold **heterogeneous** `Hashable` values, so one path can contain products, orders and settings screens together.\n\nUse a typed `[Route]` array when your routes are a single enum, which is usually cleaner: it is `Codable` for free, exhaustively switchable, and easier to test. Reach for `NavigationPath` when values genuinely come from different types you do not control. Note `NavigationPath` has its own `Codable` representation for restoration." },
    { d: 'hard', q: 'A `navigationDestination` inside a lazy container silently fails. Why?',
      a: "`navigationDestination` must be registered in a view that is **actually rendered** when the push happens. Placing it inside a `LazyVStack` row, or inside a `ForEach` body, means it may not exist yet, and SwiftUI reports 'A navigationDestination for ... was declared earlier on the stack. Only the destination declared closest to the root will be used.'\n\nThe fix is to attach destinations **once, near the root** of the stack, not per row." },
    { d: 'medium', q: 'How do you dismiss a view in SwiftUI?',
      a: "```\n@Environment(\\.dismiss) private var dismiss\n// ...\nButton(\"Close\") { dismiss() }\n```\n\nIt works for both a pushed view and a modal, dismissing whichever presentation this view belongs to. It replaced the older `presentationMode` binding. For a sheet you can also flip the `isPresented` binding from the presenter, but `dismiss` keeps the child from needing to know how it was shown." },
    { d: 'medium', q: 'Compare `sheet`, `fullScreenCover` and `popover`.',
      a: "- `.sheet(isPresented:)` or `.sheet(item:)`: a card that leaves the presenter visible, resizable with `.presentationDetents([.medium, .large])`.\n- `.fullScreenCover`: covers everything, correct for onboarding or an immersive flow.\n- `.popover`: an anchored popover on iPad and Mac, which falls back to a sheet on iPhone unless you set `.presentationCompactAdaptation`.\n\nPrefer the `item:` variants over `isPresented:` plus a separate state value, because they make it impossible to present with stale or missing data." },
    { d: 'hard', q: 'Why is `.sheet(item:)` better than `.sheet(isPresented:)`?',
      a: "With `isPresented` you keep two pieces of state, a boolean and the thing being shown, and they can disagree. Set the boolean before the value and the sheet presents with `nil` or stale content, which is a real and common crash.\n\n`.sheet(item:)` takes an optional `Identifiable`; the sheet is shown exactly when the value is non-nil and the closure receives the unwrapped value. One source of truth, no invalid state representable." },
    { d: 'hard', q: 'How would you implement deep linking with `NavigationStack`?',
      a: "Parse the URL into a typed `Route`, then set the path:\n\n```\n.onOpenURL { url in\n    guard let route = Router.parse(url) else { return }\n    path = [route]                 // or append for a deeper stack\n}\n```\n\nThe important design points are the same as UIKit: decide whether to rebuild the stack or navigate from current state, dismiss any presented sheet first since a modal blocks a push, and handle a route that requires authentication by queueing it and replaying after login." },
    { d: 'medium', q: 'What is `NavigationSplitView` for?',
      a: "Two or three column layouts on iPad and Mac, adapting to a stack on compact iPhone automatically. You give it `sidebar`, optional `content` and `detail` closures, plus a `columnVisibility` binding.\n\nThe common mistake is trying to build this from `NavigationStack` and size classes by hand. `NavigationSplitView` handles the adaptation, the column widths, and the sidebar toggle for free, and it is what makes one codebase feel native on both platforms." },
    { d: 'hard', q: 'How do you restore navigation state across launches?',
      a: "Make your route type `Codable`, persist the path, and restore it on launch. With a typed array that is trivial; `NavigationPath` exposes `codable` for the same purpose:\n\n```\nif let data = try? JSONEncoder().encode(path.codable) { ... }\n```\n\n`@SceneStorage` is the system-supported place to put it for per-scene restoration. The design payoff is the same as UIKit: one typed route serves deep links, restoration, push taps and widget taps." }
  ],
  quiz: [
    { d: 'medium', q: 'NavigationStack improved on NavigationView chiefly by:', choices: ['Faster rendering', 'Representing the whole stack as a data path you can mutate', 'Supporting more than three columns', 'Removing the need for destinations'], correct: 1,
      why: 'Programmatic navigation, deep linking and restoration all become array manipulation instead of scattered isActive bindings.' },
    { d: 'hard', q: 'A navigationDestination declared inside a ForEach row often fails because:', choices: ['ForEach cannot contain modifiers', 'It may not be rendered when the push happens; destinations belong near the root', 'It needs to be Hashable', 'Lazy containers disable navigation'], correct: 1,
      why: 'SwiftUI warns that only the destination closest to the root is used. Register destinations once on the stack.' },
    { d: 'medium', q: '`.sheet(item:)` beats `.sheet(isPresented:)` because it:', choices: ['Animates better', 'Makes an invalid presented-with-no-data state unrepresentable', 'Supports detents', 'Works on iPad'], correct: 1,
      why: 'A separate boolean and value can disagree, which is how sheets present with stale or missing content.' },
    { d: 'easy', q: 'To dismiss the current presentation from inside a child view you use:', choices: ['@Environment(\\.dismiss)', 'presentationMode.wrappedValue', 'NavigationStack.pop()', '@Binding var isPresented'], correct: 0,
      why: 'It works for both pushes and modals, and the child does not need to know how it was presented.' },
    { d: 'hard', q: 'A typed [Route] array is usually preferable to NavigationPath because it is:', choices: ['Faster', 'Codable for free, exhaustively switchable, and easier to test', 'Required by NavigationStack', 'The only option on iOS 17'], correct: 1,
      why: 'NavigationPath earns its keep only when routes genuinely span types you do not control.' },
    { d: 'medium', q: 'Two and three column iPad layouts that adapt to iPhone come from:', choices: ['NavigationStack plus size classes', 'NavigationSplitView', 'HSplitView', 'TabView'], correct: 1,
      why: 'It handles adaptation, column widths and the sidebar toggle, which is a lot to rebuild by hand.' }
  ]
});

IPREP.addTopic({
  id: 'swiftui-animation', domain: 'swiftui',
  title: 'Animation & Transitions',
  summary: 'Implicit versus explicit, transactions, matched geometry, and why animations silently do nothing.',
  cards: [
    { d: 'easy', q: 'What is the difference between implicit and explicit animation?',
      a: "**Implicit**: `.animation(.spring, value: isOpen)` attaches to a view and animates whenever that value changes. Scoped to the view it modifies.\n\n**Explicit**: `withAnimation { isOpen.toggle() }` animates every view affected by that state change, wherever it lives in the hierarchy.\n\nUse explicit when one action should animate several places, and implicit when a single view should always animate a specific property." },
    { d: 'hard', q: 'Why does `.animation()` require a `value:` parameter now?',
      a: "The old `.animation(_)` animated **any** change flowing through that view, including ones you never intended, which made behaviour unpredictable and hard to reason about as hierarchies grew. It was deprecated for exactly that reason.\n\n`.animation(_, value:)` scopes the animation to changes of a specific equatable value, so the trigger is explicit and local. If you need to animate several triggers, either use several modifiers or reach for `withAnimation` at the source." },
    { d: 'hard', q: 'What is `matchedGeometryEffect` and what does it need to work?',
      a: "It interpolates between two views that share an `id` in a common `@Namespace`, producing a hero transition:\n\n```\n@Namespace private var ns\n// small view\n.matchedGeometryEffect(id: item.id, in: ns)\n// large view\n.matchedGeometryEffect(id: item.id, in: ns)\n```\n\nRequirements: the same id, the same namespace, the change must be inside `withAnimation`, and only **one** view with that id should be the source at a time. The most common failure is both views existing simultaneously, which makes the effect jump." },
    { d: 'medium', q: 'What is a `transition` and how does it differ from an animation?',
      a: "A transition describes how a view is **inserted or removed**, for example `.opacity`, `.slide`, `.scale`, or an asymmetric pair. An animation describes how a **property changes** on a view that stays present.\n\nSo `.transition(.slide)` only takes effect when the view is actually added to or removed from the hierarchy, and only when that change is animated. A transition on a view that merely changes size does nothing, which is the usual reason it 'does not work'." },
    { d: 'hard', q: 'Give three reasons an animation silently does nothing.',
      a: "1. **The change was not animated.** A transition or geometry effect needs `withAnimation` or an `.animation(_, value:)` covering that change.\n2. **Identity changed.** If the view was replaced rather than updated, SwiftUI has nothing to interpolate; it runs a transition instead, or nothing.\n3. **The animated property is not `Animatable`.** SwiftUI interpolates values conforming to `VectorArithmetic`. A custom property needs an `animatableData` implementation.\n\nA fourth: the modifier is attached above the thing that changes, so the change never flows through it." },
    { d: 'medium', q: 'What is `Animatable` and when do you implement it?',
      a: "A protocol with an `animatableData` property SwiftUI interpolates frame by frame. Implement it when you animate something SwiftUI cannot interpolate by itself, most commonly a `Shape` driven by a number, or a text value counting up:\n\n```\nstruct Arc: Shape {\n    var angle: Double\n    var animatableData: Double {\n        get { angle } set { angle = newValue }\n    }\n}\n```\n\nWithout it, the shape jumps to its final value instead of sweeping." },
    { d: 'medium', q: 'What is a `Transaction`?',
      a: "The context carried alongside a state change, holding the animation to use and flags such as `disablesAnimations`. `withAnimation` is sugar for setting one.\n\n`.transaction { $0.animation = nil }` strips animation from a subtree, which is how you stop one part of the screen animating while everything else does. `withTransaction` gives you full control at the call site. Use it sparingly; it is a scalpel and it makes behaviour non-local." },
    { d: 'medium', q: 'How do spring animations work, and which do you pick?',
      a: "`.spring(duration:bounce:)` is the modern, intuitive spelling: duration is roughly how long it takes to settle, bounce from 0 (no overshoot) to 1 (very bouncy). The older `response`/`dampingFraction` form still exists.\n\nPresets: `.smooth` (no bounce), `.snappy` (slight bounce), `.bouncy`. Springs are interruptible and preserve velocity, which is why they feel right for gesture-driven UI where a linear ease looks wrong when interrupted." },
    { d: 'hard', q: 'What are `PhaseAnimator` and `KeyframeAnimator`?',
      a: "Both arrived in iOS 17 for multi-step animation without chained `DispatchQueue.asyncAfter` calls.\n\n`PhaseAnimator` cycles a view through a sequence of discrete phases, applying whatever styling each phase implies, and can loop or run once on a trigger. Good for a pulsing badge or a shake.\n\n`KeyframeAnimator` animates **multiple properties on independent timelines**, so scale, rotation and offset can each have their own keyframes. Good for a genuinely choreographed moment." },
    { d: 'medium', q: 'How do you respect Reduce Motion?',
      a: "```\n@Environment(\\.accessibilityReduceMotion) private var reduceMotion\n// ...\n.animation(reduceMotion ? nil : .spring, value: state)\n```\n\nThe rule is to remove or simplify **motion**, particularly large translations, scaling and parallax, not to remove all feedback. A cross-fade in place of a slide is usually the right substitution: the user still sees that something changed, without the vestibular trigger." }
  ],
  quiz: [
    { d: 'medium', q: '`.animation(_)` without a value: was deprecated because it:', choices: ['Was slow', 'Animated any change flowing through the view, including unintended ones', 'Did not support springs', 'Broke on iPad'], correct: 1,
      why: 'Scoping to a specific equatable value makes the trigger explicit and local.' },
    { d: 'hard', q: 'matchedGeometryEffect jumps instead of interpolating. Likely cause:', choices: ['Wrong animation curve', 'Both views with that id exist as sources at the same time', 'Missing .transition', 'The namespace is @State'], correct: 1,
      why: 'Only one view should be the source for an id at a time, and the change must happen inside withAnimation.' },
    { d: 'medium', q: 'A .transition has no effect on a view that only changes size because transitions apply to:', choices: ['Colour changes', 'Insertion and removal from the hierarchy', 'Layout priority', 'Gesture states'], correct: 1,
      why: 'Property changes on a persisting view are animations, not transitions.' },
    { d: 'hard', q: 'To animate a custom Shape driven by an angle you must implement:', choices: ['Equatable', 'animatableData from Animatable', 'Hashable', 'a Transaction'], correct: 1,
      why: 'Without it SwiftUI cannot interpolate the value and the shape jumps to its final state.' },
    { d: 'medium', q: 'Choreographing several properties on independent timelines is done with:', choices: ['PhaseAnimator', 'KeyframeAnimator', 'TimelineView', 'withAnimation'], correct: 1,
      why: 'PhaseAnimator steps through discrete phases; KeyframeAnimator gives each property its own keyframe track.' },
    { d: 'easy', q: 'Respecting Reduce Motion means:', choices: ['Removing all feedback', 'Replacing large motion with a simpler cue such as a cross-fade', 'Disabling the feature', 'Slowing every animation down'], correct: 1,
      why: 'The user should still perceive the change, without translation, scaling or parallax triggering the vestibular response.' }
  ]
});

IPREP.addTopic({
  id: 'swiftui-interop', domain: 'swiftui',
  title: 'UIKit Interop & Choosing Between Them',
  summary: 'Representables, hosting controllers, coordinators, and an honest account of when to use which.',
  cards: [
    { d: 'medium', q: 'What is `UIViewRepresentable` and what is its lifecycle?',
      a: "```\nstruct MapView: UIViewRepresentable {\n    func makeUIView(context: Context) -> MKMapView { MKMapView() }\n    func updateUIView(_ view: MKMapView, context: Context) { /* apply state */ }\n    func makeCoordinator() -> Coordinator { Coordinator(self) }\n}\n```\n\n`makeUIView` runs **once** to create the view. `updateUIView` runs whenever SwiftUI state the view depends on changes, and must be **idempotent**, because it will run more often than you expect. Creating objects or adding observers in `updateUIView` is a leak." },
    { d: 'hard', q: 'What is the `Coordinator` for?',
      a: "It is the object that adopts the UIKit delegate and target-action protocols, since a SwiftUI `View` struct cannot. It is created once by `makeCoordinator` and reachable as `context.coordinator`.\n\nIt is also where you bridge callbacks back into SwiftUI, usually by holding a `Binding` or a closure:\n\n```\nclass Coordinator: NSObject, UITextFieldDelegate {\n    var text: Binding<String>\n    func textFieldDidChangeSelection(_ tf: UITextField) {\n        text.wrappedValue = tf.text ?? \"\"\n    }\n}\n```" },
    { d: 'medium', q: 'What is `UIHostingController` and where do you use it?',
      a: "A `UIViewController` that hosts a SwiftUI view, which is how you go the other direction: SwiftUI inside a UIKit app. Push it, present it, or embed it as a child.\n\nIt is the main vehicle for incremental adoption. Two practical notes: use `sizingOptions` (iOS 16+) so it self-sizes to its content when embedded, and set `view.backgroundColor = .clear` if you want the SwiftUI content to sit over UIKit content, since the hosting view is opaque by default." },
    { d: 'hard', q: 'A representable has the wrong size in a SwiftUI layout. What is happening?',
      a: "SwiftUI asks the representable for a size, and by default it uses the UIKit view's intrinsic content size and constraints. A view with no intrinsic size, such as a bare `MKMapView` or `UIScrollView`, reports nothing useful and collapses or fills greedily.\n\nFixes: implement `sizeThatFits(_:uiView:context:)` (iOS 16+) to answer the proposal explicitly, give the UIKit view real content-hugging and compression-resistance priorities, or wrap it in a `.frame()` so a parent supplies the size." },
    { d: 'medium', q: 'How do you share a view model between UIKit and SwiftUI screens?',
      a: "Keep the model layer framework-agnostic and let both consume it. An `@Observable` or `ObservableObject` class works in SwiftUI directly, and in UIKit you subscribe to it: with `ObservableObject` via its Combine publisher, with `@Observable` via `withObservationTracking`.\n\nThe important discipline is that the model must not import UIKit or SwiftUI. If it does, it is a view model for one framework and you will end up with two." },
    { d: 'hard', q: 'Where is SwiftUI still genuinely weaker than UIKit?',
      a: "An honest list, which is what interviewers want rather than advocacy:\n- **Complex collection layouts.** Compositional layout still expresses things `LazyVGrid` cannot.\n- **Fine-grained scroll control.** Improved a lot in iOS 17, but precise offset manipulation and custom paging still favour UIKit.\n- **Text editing.** `TextEditor` remains limited next to `UITextView` for attributed text, input accessory views and selection control.\n- **Deep customisation of system controls**, where you often end up wrapping the UIKit equivalent anyway.\n- **Debugging.** Layout problems are harder to inspect than with the view debugger.\n\nAnd raising the deployment target is a real cost for the newest APIs." },
    { d: 'medium', q: 'Where is SwiftUI clearly better?',
      a: "- **Less code for the same screen**, especially forms, settings and list-shaped UI.\n- **State-driven rendering**, which removes a whole class of 'view and model disagree' bugs.\n- **Previews**, including multiple device and dynamic type variants at once.\n- **Dark mode, dynamic type and accessibility** largely for free.\n- **Multiplatform**, where the same view code runs on iPad, Mac, watch and TV.\n\nAnimations and transitions are also dramatically less work than the UIKit equivalents." },
    { d: 'hard', q: 'How would you adopt SwiftUI incrementally in a large UIKit app?',
      a: "Bottom-up, at leaf boundaries:\n1. Build **new, self-contained screens** in SwiftUI, hosted with `UIHostingController`, so navigation stays in UIKit.\n2. Expose **small reusable components** written in SwiftUI to the UIKit side through hosting controllers, so the design system converges.\n3. Keep the **model and navigation layers framework-agnostic** so both sides share them.\n4. Move navigation last, since it is the hardest interop seam.\n\nStart with a screen that is genuinely simple, so the first attempt teaches the team without a deadline attached." },
    { d: 'medium', q: 'What is `UIViewControllerRepresentable` for?',
      a: "The same contract as `UIViewRepresentable` but for a whole controller: `makeUIViewController`, `updateUIViewController`, and a coordinator.\n\nYou need it for anything that is fundamentally a controller: `PHPickerViewController`, `UIImagePickerController`, `SFSafariViewController`, `MFMailComposeViewController`, `UIActivityViewController`. Most 'system picker in SwiftUI' answers are this, though several now have native SwiftUI equivalents such as `PhotosPicker` and `ShareLink`." },
    { d: 'hard', q: 'What are the performance differences worth knowing?',
      a: "SwiftUI's diffing has real cost per update, so a badly structured hierarchy with a fat observable object can re-render far more than a UIKit equivalent would relayout. UIKit gives you precise control over exactly what changes.\n\nConversely SwiftUI avoids whole categories of waste: no cell reuse bugs, no manual invalidation, and `List` is backed by the same collection view underneath.\n\nThe practical answer: for typical app UI they are comparable, and for very high-frequency updates or very large custom-laid-out collections, UIKit still gives you a lower floor. Measure rather than assume." }
  ],
  quiz: [
    { d: 'medium', q: '`updateUIView` must be idempotent because it:', choices: ['Runs on a background thread', 'Runs whenever a dependency changes, more often than you expect', 'Runs only once', 'Cannot access the coordinator'], correct: 1,
      why: 'Creating objects or adding observers there leaks, because it is called repeatedly for the same view.' },
    { d: 'medium', q: 'The Coordinator exists because:', choices: ['SwiftUI views are structs and cannot be UIKit delegates', 'It improves performance', 'UIKit requires a singleton', 'It manages the view lifecycle'], correct: 0,
      why: 'It is a class that adopts the delegate protocols and bridges callbacks back through bindings or closures.' },
    { d: 'hard', q: 'A wrapped MKMapView collapses to zero height. The most direct fix is:', choices: ['Add .padding()', 'Implement sizeThatFits, or give it an explicit frame', 'Use UIViewControllerRepresentable', 'Set clipsToBounds'], correct: 1,
      why: 'It has no useful intrinsic content size, so SwiftUI has nothing to answer the proposal with.' },
    { d: 'medium', q: 'To show SwiftUI inside an existing UIKit navigation stack you use:', choices: ['UIViewRepresentable', 'UIHostingController', 'UIViewControllerRepresentable', 'A storyboard reference'], correct: 1,
      why: 'It is a UIViewController hosting a SwiftUI view, and it is the main vehicle for incremental adoption.' },
    { d: 'hard', q: 'Which is still a genuine SwiftUI weakness worth naming in an interview?', choices: ['Dark mode support', 'Rich text editing and precise scroll control', 'Animations', 'Accessibility'], correct: 1,
      why: 'TextEditor remains limited next to UITextView, and fine offset control still favours UIKit despite iOS 17 improvements.' },
    { d: 'medium', q: 'A shared view model between UIKit and SwiftUI screens must:', choices: ['Subclass UIViewController', 'Not import UIKit or SwiftUI', 'Be a singleton', 'Use Combine only'], correct: 1,
      why: 'The moment it imports a UI framework it belongs to one side, and you end up maintaining two.' }
  ]
});
