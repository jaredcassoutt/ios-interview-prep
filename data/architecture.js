/* Architecture & Design — 4 topics */

IPREP.addTopic({
  id: 'patterns', domain: 'architecture',
  title: 'MVC, MVP, MVVM & VIPER',
  summary: 'What each pattern actually separates, the trade-offs, and how to argue for one.',
  cards: [
    { d: 'easy', q: 'Why does Apple MVC turn into Massive View Controller?',
      a: "In the original MVC the controller mediates between an independent view and model. In UIKit the `UIViewController` **owns** the view, so it is a view and a controller at once. Everything with nowhere else to go lands there: networking, parsing, formatting, navigation, analytics, state machines.\n\nThe result is a 2,000-line class with no seams, untestable because it needs a view lifecycle, and unmergeable because everyone edits it. Every other pattern on this list is an answer to that one problem." },
    { d: 'medium', q: 'MVP: what moves where, and what is the defining characteristic?',
      a: "The view (the `UIViewController`) becomes passive and implements a `View` protocol with methods like `showLoading()` and `display(items:)`. The **presenter** holds the logic, receives user events, and calls back into the view protocol.\n\nThe defining characteristic is that the presenter has a **reference to the view** through a protocol. That makes it fully testable with a mock view, and it makes the data flow explicit and imperative. The cost is a lot of protocol boilerplate and two objects per screen." },
    { d: 'medium', q: 'MVVM: what changes relative to MVP?',
      a: "The view model has **no reference to the view**. It exposes state as observable properties, and the view binds to them. The direction of knowledge is reversed: the view knows the view model, not the other way around.\n\nThat makes the view model a plain object testable with no UI mock at all: set an input, assert on the output state. The binding is done with Combine, `@Published`, SwiftUI's `@Observable`, or a hand-rolled observer. The risk is the view model absorbing everything and becoming a Massive View Model." },
    { d: 'hard', q: 'VIPER: name the five parts and say honestly when it is worth it.',
      a: "**V**iew, **I**nteractor (business logic), **P**resenter (formatting and view state), **E**ntity (models), **R**outer (navigation).\n\nIt is worth it for a large app with many teams, where the strict boundaries make ownership and parallel work clear, and for flows with genuinely complex business rules that deserve their own testable layer. It is not worth it for a screen showing a list, where you write five files and three protocols to display a table.\n\nThe honest interview answer names the cost: enormous boilerplate, and a big jump in the number of files per feature." },
    { d: 'medium', q: 'What does the Coordinator add to any of these?',
      a: "Navigation. None of MVC, MVP or MVVM says who decides the next screen, so it usually falls back into the view controller, which recreates the coupling problem for flow instead of logic. A coordinator owns the flow and injects dependencies.\n\nThat is why the common real-world stack is **MVVM-C**: view models for screen logic, coordinators for flow, and view controllers reduced to rendering and forwarding events." },
    { d: 'hard', q: 'How do you actually answer \"which architecture would you use?\" in an interview?',
      a: "Do not name a pattern first. Ask about the constraints, then justify:\n- Team size and whether multiple teams touch the same feature\n- Test strategy and what level they test at\n- SwiftUI, UIKit, or both\n- Lifespan and rate of change of the feature\n\nThen give a recommendation with its costs, for example: 'MVVM-C, because it gives testable screen logic without VIPER's file count, and coordinators keep navigation out of the controllers. The risk is view models growing too large, so I would extract use-case objects for anything beyond presentation logic.'\n\nWhat is being assessed is whether you reason about trade-offs or recite acronyms." },
    { d: 'medium', q: 'What is unidirectional data flow and where does it fit?',
      a: "State flows down, events flow up, and all state changes go through a single reducer: `(State, Action) -> State`. Redux, TCA and SwiftUI's own model work this way.\n\nBenefits: one place to look for any state change, trivially replayable and testable, and time-travel debugging. Costs: boilerplate for every action, and awkwardness for genuinely imperative things like focus, scroll position and one-shot alerts. It shines for complex screens with many interacting states and is overkill for a settings toggle." },
    { d: 'hard', q: 'How do you keep a view model from becoming the new Massive View Controller?',
      a: "Give it exactly one job: turn model state into view state and turn view events into use-case calls. Everything else moves out:\n- Networking into a repository or service\n- Business rules into use-case or interactor objects\n- Formatting into formatters or a mapper\n- Navigation into a coordinator\n- Persistence behind a repository protocol\n\nA good smoke test: if the view model imports UIKit, or has more than a handful of dependencies, or its test file needs more than a couple of fakes, it is doing too much." },
    { d: 'medium', q: 'Does the pattern change for SwiftUI?',
      a: "The pressure changes. SwiftUI's `View` is already a value-type description of state, so the 'view controller does everything' failure mode does not happen the same way. Many SwiftUI apps use `@Observable` model objects with no separate view model per screen.\n\nWhat stays true: you still need somewhere for business logic that is not presentation, still need navigation ownership, and still need injectable dependencies for tests. MVVM in SwiftUI is often just 'an observable object', which is fine; adding a presenter with a view protocol is usually redundant since bindings already do that job." },
    { d: 'hard', q: 'How do you test each layer of an MVVM-C app?',
      a: "- **View model**: plain unit tests. Inject fake services, drive inputs, assert on published state. Fast, no host app.\n- **Coordinator**: inject a spy navigator, assert which screens are pushed in which order.\n- **Services / repositories**: test against a stubbed `URLProtocol` or a fake persistence layer.\n- **Views**: snapshot tests for layout regressions, and a thin layer of XCUITests for the critical happy paths only.\n\nThe shape of that pyramid is the point: many fast unit tests, a few snapshot tests, very few slow UI tests." }
  ],
  quiz: [
    { d: 'medium', q: 'The defining difference between MVP and MVVM is:', choices: ['MVP uses protocols, MVVM does not', 'The presenter holds a reference to the view; the view model does not', 'MVVM requires Combine', 'MVP has no model layer'], correct: 1,
      why: 'MVVM reverses the direction of knowledge: the view observes the view model, which knows nothing about the view.' },
    { d: 'easy', q: 'UIKit MVC degenerates into Massive View Controller mainly because:', choices: ['Objective-C has no namespaces', 'The view controller owns the view, so it is both V and C, and everything lands there', 'Storyboards are slow', 'Models cannot hold logic'], correct: 1,
      why: 'The merged V and C role leaves no natural seam, so networking, formatting and navigation all accumulate in one class.' },
    { d: 'hard', q: 'The strongest honest argument against VIPER for a simple list screen is:', choices: ['It is not testable', 'It cannot handle navigation', 'Five files and several protocols to render a table is a poor cost/benefit trade', 'It only works in Objective-C'], correct: 2,
      why: 'VIPER buys boundaries that pay off at team scale and rule complexity. On a simple screen you pay all the cost and get little back.' },
    { d: 'medium', q: 'A coordinator is added to MVVM because MVVM does not specify:', choices: ['How to fetch data', 'Who owns navigation', 'How to format dates', 'How to persist state'], correct: 1,
      why: 'Without it, flow decisions fall back into the view controller and recreate the coupling problem for navigation.' },
    { d: 'hard', q: 'The clearest sign a view model has taken on too much:', choices: ['It has published properties', 'It imports UIKit or needs many fakes to test', 'It is over 100 lines', 'It uses async/await'], correct: 1,
      why: 'Both indicate it has absorbed presentation-adjacent or infrastructure responsibilities that belong in other layers.' },
    { d: 'medium', q: 'Unidirectional data flow is least comfortable for:', choices: ['Complex interacting state', 'One-shot imperative concerns like focus, scroll position and alerts', 'Testing state transitions', 'Debugging state history'], correct: 1,
      why: 'Those are events rather than state, so they need an awkward consume-once representation inside the state tree.' }
  ]
});

IPREP.addTopic({
  id: 'di', domain: 'architecture',
  title: 'Dependency Injection & Testability',
  summary: 'Getting dependencies into objects without singletons, and what makes code testable.',
  cards: [
    { d: 'easy', q: 'What are the three forms of dependency injection?',
      a: "- **Initialiser injection**: pass dependencies to `init`. Preferred: the object is fully formed and its requirements are visible in the signature.\n- **Property injection**: set them after construction. Needed when a framework owns construction, such as a storyboard-instantiated view controller. The cost is implicitly unwrapped optionals and a window where the object is invalid.\n- **Method injection**: pass the dependency to the method that needs it. Good for a per-call dependency such as a date or a random source.\n\nDefault to initialiser injection and fall back only when the framework forces you." },
    { d: 'medium', q: 'What is actually wrong with singletons?',
      a: "Not that they are global, but that they are **implicit and unsubstitutable**. A class calling `NetworkManager.shared` has a dependency that does not appear in its interface, cannot be replaced in a test, and shares mutable state with every other caller, so tests leak into each other and order matters.\n\nA singleton is defensible for a genuinely process-wide resource, `FileManager.default`, `UserDefaults.standard`, but even then you inject it behind a protocol so the consumer does not name the singleton." },
    { d: 'medium', q: 'How do you make a legacy class with a hardcoded singleton testable?',
      a: "Default-parameter injection, which is a zero-call-site-change refactor:\n\n```\ninit(network: Networking = NetworkManager.shared) {\n    self.network = network\n}\n```\n\nProduction code keeps working unchanged, and tests pass a fake. It is the single highest-leverage refactor for a legacy iOS codebase, because you can apply it class by class without a big-bang rewrite." },
    { d: 'hard', q: 'What is a composition root and why does it matter?',
      a: "The single place where the object graph is wired: usually the app delegate, the scene delegate, or a top-level coordinator or factory. Every concrete type is chosen there, and nothing below it knows how to construct its own dependencies.\n\nIt matters because it is the difference between DI and 'passing things around'. With one composition root you can swap the whole graph for a test or a demo mode by changing one file. Without it, `SomeService()` is called in twenty places and there is no seam." },
    { d: 'medium', q: 'What is the difference between a stub, a mock, a spy and a fake?',
      a: "- **Stub**: returns canned values. No assertions.\n- **Spy**: records what it was called with, so you assert afterwards.\n- **Mock**: has expectations built in and fails if they are not met.\n- **Fake**: a real, simplified working implementation, such as an in-memory database.\n\nPrefer stubs and fakes. Heavy mocking couples the test to the implementation, so a refactor that preserves behaviour still breaks the test, which is exactly the property you do not want." },
    { d: 'hard', q: 'How do you test networking code without hitting the network?',
      a: "Inject a `URLSession` configured with a custom `URLProtocol` subclass that intercepts requests and returns canned responses:\n\n```\nlet config = URLSessionConfiguration.ephemeral\nconfig.protocolClasses = [MockURLProtocol.self]\nlet session = URLSession(configuration: config)\n```\n\nThis is better than mocking your own client protocol because it exercises the real `URLSession` path: your request building, headers, decoding and error mapping all run for real. You get integration-level confidence at unit-test speed." },
    { d: 'medium', q: 'How do you make time and randomness testable?',
      a: "Inject them. Replace `Date()` with an injected `() -> Date` or a `Clock` protocol, and `Int.random` with an injected generator. Then a test can assert on 'expired after 30 days' without waiting or sleeping.\n\nThe same principle covers `UUID()`, the locale, the time zone and the file system. Any implicit call to ambient global state is a testability hole, and time is the one that bites hardest because the tests become flaky rather than failing outright." },
    { d: 'hard', q: 'What are the arguments against a DI container framework on iOS?',
      a: "Most containers resolve by type at **runtime**, so a missing registration is a crash instead of a compile error, which throws away Swift's main advantage. They also make the graph invisible: you cannot read a file and see what depends on what, and they add a dependency and a learning curve.\n\nManual initialiser injection with a composition root gives you compile-time safety and a readable graph. Containers earn their place mainly in very large apps with many modules where manual wiring becomes genuinely unwieldy." },
    { d: 'medium', q: 'What makes code hard to test, in a checklist?',
      a: "- Constructing its own dependencies internally\n- Reaching for singletons or other global state\n- Doing work in `init`\n- Calling `Date()`, `UUID()`, `Bundle.main` or the file system directly\n- Requiring a view lifecycle to exercise logic\n- Mixing async and sync in the same method with no injection point\n- Private methods holding the interesting logic with no public seam\n\nEvery item is a missing injection point. Testability is not a separate property, it is a consequence of explicit dependencies." },
    { d: 'hard', q: 'How do you inject dependencies into a storyboard-instantiated view controller?',
      a: "Three options. Property injection right after `instantiateViewController`, which is the traditional approach and leaves the controller temporarily invalid. `instantiateViewController(identifier:creator:)` (iOS 13+), which lets you call a custom initialiser and is the correct modern answer. Or stop using storyboards for instantiation and construct the controller in code.\n\nThe `creator:` variant is worth naming specifically, because it is the one that gives you real initialiser injection with a storyboard layout." }
  ],
  quiz: [
    { d: 'easy', q: 'The preferred DI form is initialiser injection because:', choices: ['It is faster', 'The object is always fully formed and its requirements are visible in the signature', 'It avoids protocols', 'Storyboards require it'], correct: 1,
      why: 'Property injection leaves a window where the object is invalid and hides requirements from the type.' },
    { d: 'medium', q: 'The core problem with a singleton dependency is that it is:', choices: ['Slow', 'Implicit and unsubstitutable in tests', 'Thread-unsafe by definition', 'Memory intensive'], correct: 1,
      why: 'It does not appear in the interface and cannot be replaced, so shared mutable state leaks between tests.' },
    { d: 'hard', q: 'Testing networking via a custom URLProtocol is better than mocking your own client because:', choices: ['It is faster', 'Your real request building, headers, decoding and error mapping still execute', 'It requires no setup', 'It works without URLSession'], correct: 1,
      why: 'You get integration-level coverage of the code you actually wrote, at unit-test speed and with no network.' },
    { d: 'medium', q: 'Which test double records calls for later assertion?', choices: ['Stub', 'Spy', 'Fake', 'Dummy'], correct: 1,
      why: 'A stub returns canned values, a fake is a working simplified implementation, and a mock carries built-in expectations.' },
    { d: 'hard', q: 'The main argument against a runtime DI container in Swift is:', choices: ['Performance', 'A missing registration becomes a runtime crash rather than a compile error', 'It cannot handle protocols', 'It requires Objective-C'], correct: 1,
      why: 'It trades away the compile-time safety that is the main reason to use Swift for wiring in the first place.' },
    { d: 'medium', q: 'Injecting a clock rather than calling Date() directly primarily prevents:', choices: ['Memory leaks', 'Flaky, slow, time-dependent tests', 'Timezone crashes', 'Retain cycles'], correct: 1,
      why: 'Tests that wait or depend on wall-clock time are the classic source of intermittent CI failures.' }
  ]
});

IPREP.addTopic({
  id: 'storage', domain: 'architecture',
  title: 'Persistence & Storage Mechanisms',
  summary: 'UserDefaults, Keychain, files, Core Data, SwiftData and SQLite: choosing and using each.',
  cards: [
    { d: 'easy', q: 'Name the storage options and what each is actually for.',
      a: "- **UserDefaults**: small preferences, a plist loaded into memory. Kilobytes, not megabytes.\n- **Keychain**: credentials and secrets. Encrypted, survives app deletion by default.\n- **Files**: blobs, images, downloaded content, exports.\n- **Core Data / SwiftData**: object graph with relationships, querying, migration, change tracking.\n- **SQLite (or GRDB)**: when you want SQL, deterministic performance and full control.\n- **CloudKit**: sync across a user's devices with the system account.\n\nThe interview trap is putting big or sensitive data in `UserDefaults`." },
    { d: 'medium', q: 'Why should you not store large data or secrets in UserDefaults?',
      a: "It is a **plist file** in the app container, read fully into memory at first access and rewritten on change. Large values mean slower launch, higher memory, and expensive writes.\n\nIt is also **unencrypted and trivially readable** from a device backup or a jailbroken device, so tokens, passwords and personal data do not belong there. Use the Keychain for secrets and files or a database for anything large." },
    { d: 'medium', q: 'What do the Keychain accessibility classes mean?',
      a: "- `kSecAttrAccessibleWhenUnlocked`: readable only while the device is unlocked. The sensible default.\n- `...AfterFirstUnlock`: readable after the first unlock following boot, which is what background tasks need.\n- `...WhenUnlockedThisDeviceOnly`: additionally excluded from backups, so it never restores onto another device.\n- `...WhenPasscodeSetThisDeviceOnly`: requires a passcode to exist, and the item is destroyed if the passcode is removed.\n\nThe `ThisDeviceOnly` distinction is the one interviewers probe: it is what stops a refresh token migrating to a restored device." },
    { d: 'hard', q: 'What are the Core Data concurrency rules?',
      a: "An `NSManagedObjectContext` and its managed objects are bound to a single queue. The rules:\n- Use `perform` or `performAndWait` for **every** access to a context that is not obviously on its own queue.\n- Never pass an `NSManagedObject` between contexts. Pass its `NSManagedObjectID` and re-fetch.\n- A view context is main-queue; background work uses `newBackgroundContext()` or `performBackgroundTask`.\n- Merge changes into the view context with `automaticallyMergesChangesFromParent` or by observing the save notification.\n\nViolating any of these gives you the classic 'accessed on the wrong queue' crash, which often surfaces far from the cause." },
    { d: 'hard', q: 'What is a faulted managed object and why does it matter for performance?',
      a: "Core Data returns objects as **faults**: shells with an object ID and no property data. Touching a property triggers a fault-fire, which is a round trip to the store.\n\nSo a loop over 500 fetched objects reading one property each performs 500 round trips. Fix it with `fetchRequest.returnsObjectsAsFaults = false`, `relationshipKeyPathsForPrefetching` for relationships, and `fetchBatchSize` to page. Recognising the N+1 pattern here is exactly what a Core Data question is testing." },
    { d: 'medium', q: 'What are the Core Data migration strategies?',
      a: "- **Lightweight migration**: Core Data infers the mapping. Covers adding and removing attributes and entities, renaming with a renaming identifier, and changing optionality. Enable with `shouldMigrateStoreAutomatically` and `shouldInferMappingModelAutomatically`.\n- **Mapping model**: a visual mapping for changes it cannot infer.\n- **Custom migration policy**: an `NSEntityMigrationPolicy` subclass for transformations needing code.\n- **Progressive migration**: chained model versions when users may skip several app versions.\n\nThe production risk is a user on a two-year-old version needing three migrations at once, on a large store, during launch. Test that path." },
    { d: 'hard', q: 'When would you choose SQLite or GRDB over Core Data?',
      a: "- You need complex queries, joins and aggregates that `NSPredicate` expresses awkwardly.\n- You need predictable performance and to see the actual SQL.\n- You want the schema shared with a backend or another platform.\n- You want simple, testable value types instead of `NSManagedObject` subclasses with their lifecycle rules.\n\nYou give up: free change tracking with `NSFetchedResultsController`, the graph and relationship management, undo support, and CloudKit sync integration. It is a real trade, not a clear win." },
    { d: 'medium', q: 'How do you choose where in the file system to write?',
      a: "- `Documents`: user-generated content that must persist and be backed up.\n- `Library/Application Support`: app-generated data the user did not create. Backed up.\n- `Library/Caches`: reproducible data. **The system may delete it under disk pressure**, so code must handle it disappearing.\n- `tmp`: scratch, deleted whenever.\n\nBacked-up locations count against the user's iCloud quota, so downloaded content that can be re-fetched belongs in Caches or must be flagged with `isExcludedFromBackup`. App Review has rejected apps for backing up re-downloadable content." },
    { d: 'hard', q: 'How would you design an offline-first cache layer?',
      a: "A repository that returns local data immediately and refreshes in the background:\n\n1. Read from the store and publish it, so the UI is never empty.\n2. Kick off a network fetch with an ETag or `If-Modified-Since`.\n3. On 200, write through to the store; the UI updates through the same observation path.\n4. On 304 or an error, keep local data and surface a subtle staleness indicator.\n5. Queue mutations locally with a pending flag and replay on reconnect.\n\nThe hard part is **conflict resolution**: last-write-wins is simplest, per-field merge is better, and a server-authoritative version number avoids most of it. Say which you would pick and why." },
    { d: 'medium', q: 'What is SwiftData and how does it relate to Core Data?',
      a: "A Swift-native layer over the same persistence machinery, using macros (`@Model`, `@Query`) instead of a visual model editor and `NSManagedObject` subclasses. It gives value-type-feeling models, compile-time-checked queries and much less boilerplate, and integrates directly with SwiftUI.\n\nIt is younger, so it has gaps around complex migrations, fine-grained fetch tuning and some concurrency scenarios, and it raises the minimum OS version. For a new SwiftUI app it is a reasonable default; for a large existing Core Data stack the migration is rarely worth it yet." }
  ],
  quiz: [
    { d: 'easy', q: 'An auth token belongs in:', choices: ['UserDefaults', 'Keychain', 'A file in Documents', 'Core Data'], correct: 1,
      why: 'UserDefaults is an unencrypted plist readable from a backup. The Keychain is encrypted and has accessibility controls.' },
    { d: 'hard', q: 'To stop a refresh token restoring onto a different device you use:', choices: ['kSecAttrAccessibleWhenUnlocked', 'kSecAttrAccessibleAfterFirstUnlock', 'kSecAttrAccessibleWhenUnlockedThisDeviceOnly', 'kSecAttrAccessibleAlways'], correct: 2,
      why: 'ThisDeviceOnly excludes the item from backups, so it cannot migrate during a device restore.' },
    { d: 'medium', q: 'Passing an NSManagedObject between contexts is wrong because:', choices: ['It is slow', 'Managed objects are bound to their context queue; pass the objectID and re-fetch', 'It duplicates the row', 'It breaks faulting'], correct: 1,
      why: 'The object and its context are queue-bound. Violations produce wrong-queue crashes that surface far from the cause.' },
    { d: 'hard', q: 'A loop over 500 fetched Core Data objects is slow. Most likely:', choices: ['The predicate is unindexed', 'Faults firing one per object, an N+1 pattern', 'The context is on the main queue', 'The store is not SQLite'], correct: 1,
      why: 'Use returnsObjectsAsFaults = false, relationshipKeyPathsForPrefetching, and fetchBatchSize to collapse the round trips.' },
    { d: 'medium', q: 'Re-downloadable content must not be stored in Documents because:', choices: ['It is slower', 'It is backed up, counting against iCloud quota, and App Review rejects this', 'It is read-only', 'It is deleted on update'], correct: 1,
      why: 'Use Library/Caches, or set isExcludedFromBackup on the file.' },
    { d: 'hard', q: 'The hardest part of an offline-first design is usually:', choices: ['Choosing a database', 'Conflict resolution when local and server state diverge', 'Serialising to JSON', 'Background fetch scheduling'], correct: 1,
      why: 'Last-write-wins, per-field merge and server version numbers are the options. Naming the trade-off is the expected answer.' }
  ]
});

IPREP.addTopic({
  id: 'network', domain: 'architecture',
  title: 'Network Layer & Mobile API Design',
  summary: 'Building a URLSession layer, and designing the API a mobile client actually wants.',
  cards: [
    { d: 'easy', q: 'What are the layers of a well-factored network stack?',
      a: "1. **Endpoint / request builder**: a typed description of path, method, headers and body. No networking.\n2. **Client**: takes an endpoint, builds a `URLRequest`, executes it, returns `Data` plus the response. Handles retries, auth injection and logging.\n3. **Decoder / mapper**: `Data` to domain models, mapping transport and server errors into a domain error type.\n4. **Repository / service**: composes the above into meaningful operations and owns caching.\n\nThe test of the design is whether you can unit test each layer without a network." },
    { d: 'medium', q: 'What does URLSession give you for free that people reimplement badly?',
      a: "- Connection pooling and HTTP/2 multiplexing\n- A shared `URLCache` honouring `Cache-Control` and `ETag`\n- Background transfers that continue after the app is suspended or killed\n- Cellular and expensive-network policies via `allowsCellularAccess`, `allowsExpensiveNetworkAccess`, `waitsForConnectivity`\n- Automatic retry of idempotent requests on connection reuse failures\n- System proxy, TLS and certificate handling\n\nMost hand-rolled clients lose the cache and the background support, which is why the answer to 'would you use Alamofire?' is usually 'not for the transport'." },
    { d: 'medium', q: 'How do you design the error type for a network layer?',
      a: "A domain enum that hides transport details from callers but keeps enough to act on:\n\n```\nenum APIError: Error {\n    case offline\n    case timeout\n    case unauthorized\n    case server(status: Int, message: String?)\n    case decoding(DecodingError)\n    case cancelled\n}\n```\n\nThe point is that a view model can decide 'show retry' versus 'send to login' versus 'log and show generic' without inspecting `URLError` codes. Leaking `URLError` and `DecodingError` to the UI layer is a design smell." },
    { d: 'hard', q: 'How do you implement token refresh without a thundering herd?',
      a: "Serialise refresh through a single in-flight task:\n\n```\nactor TokenProvider {\n    private var refreshTask: Task<Token, Error>?\n    func token() async throws -> Token {\n        if let t = current, !t.isExpired { return t }\n        if let task = refreshTask { return try await task.value }\n        let task = Task { try await refresh() }\n        refreshTask = task\n        defer { refreshTask = nil }\n        return try await task.value\n    }\n}\n```\n\nTen concurrent 401s then trigger **one** refresh and all await it. Also: retry the original request exactly once after a refresh, and on a refresh failure log the user out rather than looping." },
    { d: 'hard', q: 'Design a retry policy. What must it include?',
      a: "- **Only idempotent requests** by default. Retrying a POST can double-charge; require an idempotency key for those.\n- **Exponential backoff with jitter**, so a server recovering from an outage is not hit by every client in lockstep.\n- **A cap** on attempts and total elapsed time.\n- **Retry only the right conditions**: 5xx, 429 with `Retry-After`, and connectivity errors. Never 4xx other than 429, since the request will not succeed.\n- **Respect cancellation** so a dismissed screen stops retrying.\n\nJitter is the detail interviewers listen for, because without it retries synchronise into a self-inflicted DDoS." },
    { d: 'medium', q: 'What are the mobile-specific concerns when designing an API?',
      a: "- **Round trips are expensive**: a cold cellular request can be hundreds of milliseconds before a byte moves. Prefer one composite response over five chatty calls.\n- **Payload size costs battery and data**: the radio stays powered for a tail after each transfer.\n- **Clients cannot be force-updated**: the API must be versioned and backward compatible for years.\n- **Offline is normal**, not exceptional.\n- **Push should carry a hint, not a payload**, since delivery is best-effort.\n\nThe headline: design for high latency and intermittent connectivity, not for bandwidth." },
    { d: 'hard', q: 'Compare pagination strategies for a mobile feed.',
      a: "- **Offset/limit**: simple, but items shift when new content is inserted, so users see duplicates or skip items. Also gets slower deep into the list.\n- **Cursor / keyset**: the server returns an opaque cursor for the next page. Stable under insertion, efficient at any depth. **The right default.**\n- **Time-based**: a cursor variant using a timestamp; breaks on ties and clock skew.\n\nAlso specify what happens when the client has stale cursors after days offline, and give the client a way to refresh from the top without re-paging the whole list." },
    { d: 'hard', q: 'How do you version a mobile API, and what does backward compatibility require?',
      a: "URL versioning (`/v1/`) is the clearest for major breaks. Within a version, evolve **additively**: new optional fields are safe, removing or renaming a field or changing its type is not.\n\nThe client's half of the contract is to be **tolerant**: unknown fields ignored, missing optional fields defaulted, unknown enum cases mapped to an `unknown` case rather than failing the decode. That last one is the most common real-world outage: the server adds a new status string and every old client fails to parse the whole response." },
    { d: 'medium', q: 'When would you choose GraphQL over REST for a mobile client, honestly?',
      a: "GraphQL wins when screens need different subsets of a large graph and REST would force over-fetching or many round trips, and when several clients with different needs share one backend.\n\nIt costs you: HTTP caching no longer works out of the box since everything is one POST endpoint, query complexity can hurt the server, error handling is per-field rather than per-response, and the tooling and schema discipline are a real investment. For a small app with stable screens, REST plus a couple of composite endpoints is usually the better trade." },
    { d: 'medium', q: 'What is certificate pinning, and when is it worth it?',
      a: "Validating that the server's certificate or public key matches one you shipped, rather than trusting any CA. It defends against a compromised or coerced CA and against a user-installed proxy certificate.\n\nWorth it for banking, health and anything where a MITM is a serious threat. The cost is operational: if you pin a leaf certificate and it rotates before your app update ships, every client breaks at once. Pin the **public key**, pin a backup key, and ship a remote kill switch." },
    { d: 'hard', q: 'How would you design image loading for a feed, end to end?',
      a: "1. **Request the right size**: ask the CDN for a variant sized to the display, not the original.\n2. **Two-tier cache**: an in-memory `NSCache` of decoded images keyed by URL plus size, and a disk cache honouring HTTP caching.\n3. **Decode off the main thread**, downsampling with `CGImageSourceCreateThumbnailAtIndex` so a full-resolution bitmap never exists.\n4. **Cancel on reuse**, tied to `prepareForReuse` and the prefetch API.\n5. **Bound concurrency** so a fast scroll does not open fifty connections.\n6. **Progressive or blurhash placeholder** to avoid layout shift.\n\nMentioning downsampling and cancellation is what separates a real answer from a description of a URL loading loop." }
  ],
  quiz: [
    { d: 'medium', q: 'The detail that stops retries becoming a self-inflicted DDoS is:', choices: ['A shorter timeout', 'Exponential backoff with jitter', 'Retrying on 4xx too', 'A larger connection pool'], correct: 1,
      why: 'Without jitter every client retries in lockstep and hits the recovering server simultaneously.' },
    { d: 'hard', q: 'Ten concurrent requests get 401. The correct design triggers:', choices: ['Ten refreshes', 'One refresh that the other nine await', 'An immediate logout', 'Ten retries with no refresh'], correct: 1,
      why: 'Serialise refresh through a single in-flight task. Retry each original request once after it resolves.' },
    { d: 'medium', q: 'Cursor pagination beats offset pagination on mobile mainly because:', choices: ['It uses less bandwidth', 'It is stable when items are inserted, so users do not see duplicates or gaps', 'It supports sorting', 'It is required by REST'], correct: 1,
      why: 'Offsets shift under insertion and degrade in performance deep into a list.' },
    { d: 'hard', q: 'The most common real-world break from an additive server change is:', choices: ['A new endpoint', 'A new enum case the client decodes strictly and fails on', 'A larger payload', 'A new header'], correct: 1,
      why: 'Map unknown raw values to an unknown case. Strict enum decoding turns an additive change into a total parse failure.' },
    { d: 'medium', q: 'Pinning a leaf certificate rather than a public key is risky because:', choices: ['It is slower to validate', 'Certificate rotation breaks every shipped client at once', 'It does not stop MITM', 'iOS does not support it'], correct: 1,
      why: 'Pin the public key, include a backup pin, and ship a kill switch. Clients cannot be force-updated.' },
    { d: 'hard', q: 'The image-loading detail that most reduces memory in a feed is:', choices: ['A larger NSCache limit', 'Downsampling during decode so a full-resolution bitmap never exists', 'Using JPEG instead of PNG', 'Prefetching more aggressively'], correct: 1,
      why: 'A decoded bitmap costs width x height x 4 bytes regardless of file size. Decode straight to display size.' },
    { d: 'easy', q: 'Which does URLSession give you that hand-rolled clients most often lose?', choices: ['TLS', 'HTTP caching and background transfers', 'JSON decoding', 'Retry logic'], correct: 1,
      why: 'URLCache with ETag support and background sessions that survive suspension are both non-trivial to replace.' }
  ]
});
